# Status Toasts

Status-colored toast variants (success, error, warning, info).

## Usage
```bash
npx devsnips add Tailwind/Components/Toasts/status-toasts
```
Copy from `code.html`. Do not rely on color alone for status.

## File Structure
`code.html` · `preview.html` · `metadata.json` · `README.md`
