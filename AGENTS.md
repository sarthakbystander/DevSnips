# DevSnips ‚Äî Repository Knowledge

## CURRENT AUTHORITATIVE STATE (2026-08-13 Tailwind three-type IA migration -> Components + Sections + Templates)
**The filesystem is the source of truth.** `snippets-index.json` is regenerated from the migrated filesystem by `_gen/rebuild_index.py` and validated by `scripts/validate.py`. Do NOT trust stale "Stats after" snapshots elsewhere in this file or in README/CHANGELOG history (pre-migration entries reference the old merged Sections-into-Components layout).
- **Tailwind architecture: three first-class content types.** `Tailwind/` contains `Components/`, `Sections/`, and `Templates/` as sibling directories. Sections are NOT merged into Components; Templates are NOT Components. This reverses the prior 2026-08-10 merge for Tailwind only.
- **Vanilla/React architecture: two content types** (`Components/` + `Templates/`). `Sections/` is a Tailwind-only content type; `scripts/validate.py` forbids `Vanilla/Sections/` and `React/Sections/`.
- **Grand total: 681 content items across 87 families.** (`snippets-index.json` stats: `totalFamilies=87`, `totalVariants=681`, `totalStyles=1505`, plus `stats.tailwindByType={component:253, section:200, template:9}`) — the 2026-08-15 `_gen/rebuild_index.py` run also cleaned up pre-existing stale Component index entries (Modals/Navbar/Navigation/Tables/Tabs/Toasts/Tooltips variants that were indexed but not on disk).
  - Tailwind CSS: 462 variants across 38 families: Components 253 (13 families), Sections 200 (16 families), Templates 9 (9 families).
  - Vanilla HTML/CSS/JS: 219 variants (Components 195, Templates 24) across 49 families (32 Components + 17 Templates).
  - React: `React/Components/` now contains the **Buttons** family (30 components). **2026-08-17 TypeScript + Tailwind migration** migrated the entire Buttons family from `code.jsx` (plain JSX + inline `--ds-*` CSS classes) to a **TypeScript-first, Tailwind-first** stack. Each button asset = `React/Components/Buttons/<kebab-name>/{code.tsx, code.jsx, preview.html, metadata.json, README.md}`. `code.tsx` is the PRIMARY implementation: intentional TypeScript APIs (proper prop interfaces extending `ButtonHTMLAttributes`/`AnchorHTMLAttributes`/`HTMLAttributes`, discriminated-union variants, `ButtonSize` shared type), NO `any`, and Tailwind utility classes consuming the `--ds-*` semantic tokens via arbitrary values (`bg-[var(--ds-color-primary)]`, `rounded-[var(--ds-radius-sm)]`, `hover:bg-[color-mix(in_srgb,var(--ds-color-primary)_88%,#000)]` for hover darkening — the only `#000`/hardcoded hex allowed, inside `color-mix`). `code.jsx` is the JavaScript parity build, derived from `code.tsx` via esbuild (TS types stripped, `export` restored) — same API/behavior/classes, no TS-only syntax. `preview.html` is a self-contained runnable demo: loads Tailwind CDN + React 18 UMD + Babel standalone, defines the `--ds-*` token `:root`/`[data-theme="dark"]` block (mirrors `React/DESIGN_TOKENS.md`), inlines a shared `Icon` set + `useClickOutside`/`useCopy` hooks + the actual component (auto-transformed from `code.tsx`, `export`/`import` stripped, wrapped in an IIFE exposing it on `window` so sibling helpers don't collide) + a per-component `Showcase`, with a persisted no-flash light/dark toggle. No component-specific CSS files; no inline `style=` in `code.tsx`/`code.jsx`; no purple/neon/gradient/glassmorphism/decorative blobs/emoji. Real interactions genuinely work (split-button keyboard-navigable menu with Escape/outside-click, copy `aria-live` feedback, toggle/toggle-group `aria-pressed` + arrow-key roving, segmented-button radiogroup semantics, pagination windowing + bound-disabled Prev/Next, loading/refresh/download spinners with `aria-busy`, command-button ⌘K global shortcut). Shared visual system: 36px md default height, `radius-sm` buttons / `radius-md` menus, `focus-visible` ring via `color.focus-ring`, `disabled:opacity-50` + `pointer-events-none`, `motion-reduce:transition-none` guards, icon sizing via `[&_svg]:size-*` tokens (14–20px). The 30 buttons: solid, outline, secondary, ghost, link, destructive, success, icon, loading, button-with-icon, button-with-chevron, split, button-group, segmented, toggle, toggle-group, close, back, add, filter, sort, export, download, copy, refresh, more-actions, command, floating-action, sticky-action, pagination. metadata.json uses `technology:"react"`, `type:"component"`, `category:"Buttons"`, `subcategory` ∈ {Primary, Secondary, Navigation, Selection, Feedback, Utility, Composite}, **`languages:["JSX","TSX"]`**, **`styling:"Tailwind CSS"`**, `framework:"React"`, `language:"TSX"`. React is NOT yet a registered technology in `snippets-index.json` (index registration is a follow-up; `scripts/validate.py` passes). The migration is generated by `python3 _gen_react_buttons.py` (single declarative registry in `_gen_react_buttons_registry.py`; esbuild in `/tmp/dsbuild` is a build-time-only tool, not committed), QA'd by `python3 _qa_react_button.py <slug>` (Playwright: overflow×{375,768,1280}, light/dark token switch, focus-visible, disabled), parity-checked by `python3 _check_react_parity.py`, and audited by `python3 _audit_react_buttons.py` (consistency + anti-AI). Re-run `python3 _gen_react_buttons.py --check` to detect drift.
  - **React/Components/Checkboxes/ (12 checkboxes, 2026-08-18) + React/Components/Radios/ (10 radios, 2026-08-18):** the next React component families after Buttons/Inputs/Selects, following the exact same `{code.tsx, code.jsx, preview.html, metadata.json, README.md}` asset shape and TypeScript-first/Tailwind-first stack. Each is a REAL native `<input type="checkbox">` / `<input type="radio">` (no div-based fakes) — the native input carries the visual indicator, value, and all native behavior (Space toggles, arrow-key radio navigation, form submission). Shared visual system: 18px control, `radius-xs` (checkbox) / `rounded-full` (radio), 1px `color.border`, check-mark/dot glyph whose opacity tracks the tracked `isChecked` state (sibling `<span aria-hidden>` — NOT a `peer-checked` variant, so it works in both controlled and uncontrolled modes), `focus-visible` ring via `color.focus-ring`, `disabled:opacity-50`, `motion-reduce:transition-none`. Controlled (`checked`/`onChange`) AND uncontrolled (`defaultChecked`) modes both supported. Checkboxes (12): checkbox (reference), checkbox-with-label, checkbox-with-helper, checkbox-with-error (`aria-invalid` + `role="alert"`, not color alone), checkbox-with-description, checkbox-disabled, checkbox-readonly (real read-only via `readOnly` + `preventDefault` — browsers don't honor `readOnly` on checkboxes natively), checkbox-indeterminate (REAL `.indeterminate` IDL property set imperatively on the DOM node via `useEffect`+`useRef` — no HTML attribute exists — renders a dash, distinct from the check mark), checkbox-group (`<fieldset>`/`<legend>`, value array, controlled+uncontrolled), checkbox-card (whole card is a clickable `<label htmlFor>`), checkbox-card-group (grid 1/2/3 cols, collapses to 1 below `sm`), checkbox-with-select-all (master reflects checked/indeterminate/unchecked child state; `.indeterminate` set imperatively; disabled children excluded from all/none calc). Radios (10): radio (reference), radio-with-label, radio-with-helper, radio-with-error, radio-with-description, radio-disabled, radio-group (`<fieldset>`/`<legend>`, only one selected, native arrow-key nav), radio-card, radio-card-group, radio-with-icons (optional ReactNode leading icon that communicates meaning — NOT decorative; icons are `aria-hidden`, label carries accessible name). No component-specific CSS files; no `peer-checked` (controlled-mode-incompatible); no purple/neon/gradient. Generated by `python3 _gen_react_checkboxes.py` + `python3 _gen_react_radios.py` (one shared registry `_gen_react_checkboxes_radios_registry.py`; reuses the buttons preview architecture — `_tsx_to_babel_component` + `TOKEN_BLOCK` + `TAILWIND_CONFIG` + `PREVIEW_CSS` + `ICON_JS` from `_gen_react_buttons.py`; esbuild in `/tmp/dsbuild` build-time-only, not committed). `--check` flags drift. QA'd by `python3 scripts/_qa_react_checkboxes_radios.py` (Playwright: real `.indeterminate` IDL assertion, select-all master reflects children across checked/indeterminate/unchecked, radio arrow-key exclusivity, card-click toggles, `aria-invalid`/`role="alert"`, focus-visible outline width, dark-mode token flip, 0 horizontal overflow @ 375/768/1280, 0 console errors — ALL PASSED). metadata.json: `technology:"react"`, `type:"component"`, `category:"Checkboxes"`/`"Radios"`, `languages:["JSX","TSX"]`, `styling:"Tailwind CSS"`. React still NOT registered in `snippets-index.json` (follow-up; `scripts/validate.py` passes — it only scans Tailwind/Vanilla trees).
  - **React/Components/Switches/ (12 switches, 2026-08-19):** the next React component family after Buttons/Inputs/Selects/Checkboxes/Radios, following the exact same `{code.tsx, code.jsx, preview.html, metadata.json, README.md}` asset shape and TypeScript-first/Tailwind-first stack. Each is a REAL native `<input type="checkbox" role="switch">` with an explicit `aria-checked` (no div/button-based fakes) — the native input carries the value and all native behavior (Space toggles, form submission, disabled semantics); switch turn-on semantics come from `role="switch"`. Shared visual system (established by the `switch` reference, honored in every variant): 24×14px track (DESIGN_TOKENS "Switch: default 24×14"), 10px thumb, 2px inset, 10px travel (`translate-x-[10px]` off→on, tracked from the tracked `isChecked` state — NOT a `peer-checked` variant, so controlled and uncontrolled modes both work), full radius, unchecked `bg-input`/`border-border` + `hover:border-strong`, checked `bg-primary`/`border-primary`, unchecked thumb `bg-muted-foreground` → checked thumb `bg-primary-foreground`, `focus-visible:outline-2 outline-offset-2` via `color.focus-ring`, wrapper `opacity-50` when disabled (the whole track+thumb fades together — NOT `disabled:opacity-50` on the input alone), error state = destructive border always + destructive fill when checked, `motion-reduce:transition-none`, `transition-[transform,background-color] duration-150 ease-out`. Controlled (`checked`/`onChange`) AND uncontrolled (`defaultChecked`) modes both supported on every variant. Switches (12): switch (reference), switch-with-label, switch-with-description, switch-with-helper (`pl-[34px]` indent = 24 track + 10 gap — checkbox used `pl-[26px]` for its 18px control, this follows the analogous calc), switch-with-error (`aria-invalid` + `role="alert"`, not color alone), switch-disabled, switch-loading (`loading` prop — input disabled + `aria-busy=true` while pending, thumb replaced by a same-geometry spinner svg so there is NO layout shift; spinner represents a real async save, not decoration; demo showcase holds checked until save resolves), switch-group (`<fieldset>`/`<legend>`, value array of ON options, each switch independently controllable — NOT radio semantics), switch-card (whole row is a clickable `<label htmlFor>`, primary border when on), switch-card-group (grid 1/2 cols, collapses to 1 below `sm`), switch-with-icon (optional ReactNode leading icon that communicates meaning — NOT decorative; state color is supplementary, never color alone), switch-with-status (tracked text readout "Enabled"/"Disabled" via `onText`/`offText`, wired with `aria-describedby` — state readable in words, never color alone). No component-specific CSS files; no `peer-checked`; no purple/neon/gradient/glassmorphism/emoji. Generated by `python3 _gen_react_switches.py` (registry in `_gen_react_switches_registry.py`; reuses the buttons preview architecture — `_tsx_to_babel_component` + `TOKEN_BLOCK` + `TAILWIND_CONFIG` + `PREVIEW_CSS` + `ICON_JS` from `_gen_react_buttons.py`; esbuild in `/tmp/dsbuild` build-time-only, not committed). `--check` flags drift. QA'd by `python3 scripts/_qa_react_switches.py` (Playwright: role=switch + aria-checked on every variant, click + Space toggles, label click toggles, aria-invalid + role=alert error resolves on toggle, native disabled stays fixed, aria-busy blocks interaction until the async save resolves, group independence, card click toggles, card-group 1↔2 column responsive, status text flips, focus-visible 2px outline, dark-mode token flip, reduced-motion transition-none, 0 horizontal overflow @ 375/768/1280, 0 console errors — ALL PASSED). TSX strict-checked with `tsc` (typescript@7 in /tmp/dsbuild, `/tmp/dsbuild/src` copies, react-jsx + bundler resolution + @types/react 18); TSX↔JSX API parity verified via `_check_react_parity.py`'s signature algorithm (adapted inline for Switches). metadata.json: `technology:"react"`, `type:"component"`, `category:"Switches"`, `languages:["JSX","TSX"]`, `styling:"Tailwind CSS"`. React still NOT registered in `snippets-index.json` (follow-up; `scripts/validate.py` passes — it only scans Tailwind/Vanilla trees).
  - **React/Components/Textareas/ (10 textareas, 2026-08-19):** the next React component family after Buttons/Inputs/Selects/Checkboxes/Radios/Switches, following the exact same `{code.tsx, code.jsx, preview.html, metadata.json, README.md}` asset shape and TypeScript-first/Tailwind-first stack. Each is a REAL native `<textarea>` (no contenteditable fakes) — the native element carries all native behavior (form submission, keyboard, selection, copy/paste, `name`/`value`, `required`, `disabled`, `readOnly`, `minLength`/`maxLength`). Shared visual system (established by the `textarea` reference, honored in every variant): full width, `min-h-[80px]` floor (~3 rows of body-md), `resize-y` (vertical-only — the intentional DevSnips resize behavior; horizontal resize off so layouts never break), `rounded-[var(--ds-radius-sm)]`, 1px `color.border` (`focus:border-strong` emphasis), `bg-[var(--ds-color-input)]` surface with hover/focus surface shift (`input-hover`/`input-focus` with fallbacks), `px-3 py-2` padding, `text-sm leading-5` (body-md), muted placeholder, `focus-visible:outline-2 outline-offset-2` via `color.focus-ring`, disabled = native `:disabled` muted surface + `opacity-60` + `pointer-events-none`, read-only = native `:read-only` subtle surface + muted text (distinct from disabled), error = destructive border + `aria-invalid` + `role="alert"` message (not color alone), `transition-colors duration-150 ease-out` + `motion-reduce:transition-none`. Controlled (`value`/`onChange`) AND uncontrolled (`defaultValue`) usage work natively (no duplicated state except where a derivative is tracked — counter/auto-resize/actions track internal value only when uncontrolled). Textareas (10): textarea (reference, optional label like the switch reference), textarea-with-label (required `<label htmlFor>` + `required` asterisk), textarea-with-description (muted description between label and control, `aria-describedby`), textarea-with-helper (muted helper below control, `aria-describedby`), textarea-with-error (`error` prop drives destructive border + `aria-invalid` + `role="alert"` message, clears live), textarea-disabled (real `disabled` default true — not focusable, not submitted; label gets not-allowed cursor), textarea-readonly (real `readOnly` default true — focusable, selectable, submitted; visually distinct from disabled), textarea-with-counter (live `current / maximum` when `maxLength` supplied — NO default, so plain count when omitted; count derives from the real value, `aria-describedby` + `aria-live="polite"` region, at-limit emphasis = weight + color, not color alone), textarea-auto-resize (real JS measurement: `useRef` + `useEffect` on the resolved value, `height:auto`→`min(scrollHeight, maxHeight)` (default 320px cap → scrolls), grows/shrinks, handles initial value + controlled changes, ONE window-resize listener + `document.fonts.ready` re-measure to fix the pre-font over-measure race, `resize-none` since the component owns the height, `overflow-y-auto`, instant height changes = reduced-motion-safe, rows=3 fallback if effects never run), textarea-with-actions (action bar below: live `current / maximum` count + real ghost `Clear` (empties value + refocuses; controlled mode needs parent `onClear`) + secondary `Copy` (clipboard with execCommand fallback, label swap + `role="status"`/`aria-live` announcement; both `disabled` when empty; action row `flex-wrap` for mobile)). No component-specific CSS files; no `peer-checked`; no purple/neon/gradient/glassmorphism/emoji. Generated by `python3 _gen_react_textareas.py` (registry in `_gen_react_textareas_registry.py`; reuses the buttons preview architecture — `_tsx_to_babel_component` + `TOKEN_BLOCK` + `TAILWIND_CONFIG` + `PREVIEW_CSS` + `ICON_JS` from `_gen_react_buttons.py`; esbuild in `/tmp/dsbuild` build-time-only, not committed). `--check` flags drift. QA'd by `python3 scripts/_qa_react_textareas.py` (Playwright + static: metadata JSON validity, TSX/JSX signature parity, no `any`; real textarea rendered per variant, label click focuses, `aria-describedby` links description/helper/error, `aria-invalid`+`role=alert` + live error resolution, native disabled blocks input, real readOnly blocks edits but stays focusable, counter tracks real value + at-limit, auto-resize grows/shrinks/caps at maxHeight + resize-none, Clear empties+refocuses + disabled-when-empty, Copy label swap + role=status announcement, focus-visible 2px outline, dark-mode token flip, reduced-motion transition-none, 0 horizontal overflow @ 375/768/1280, 0 console errors — ALL PASSED). TSX strict-checked with `tsc` (typescript@7 in /tmp/dsbuild, `/tmp/dsbuild/src` slug-named copies, react-jsx + bundler resolution + @types/react 18 — gotcha: extending `TextareaHTMLAttributes` collides with native `onCopy` clipboard-event prop, solved with `Omit<..., "onCopy">` like the copy-button's `Omit<..., "onClick">`). metadata.json: `technology:"react"`, `type:"component"`, `category:"Textareas"`, `subcategory` ∈ {Core, Labeling, States, Feedback, Behavior, Composite}, `languages:["JSX","TSX"]`, `styling:"Tailwind CSS"`. React still NOT registered in `snippets-index.json` (follow-up; `scripts/validate.py` passes — it only scans Tailwind/Vanilla trees).
  - **React/Components/Tabs/ (11 tabs, 2026-08-21):** the next React component family after Buttons/Inputs/Selects/Checkboxes/Radios/Switches/Textareas, following the exact same `{code.tsx, code.jsx, preview.html, metadata.json, README.md}` asset shape and TypeScript-first/Tailwind-first stack. FIRST multi-export family — a real compound component (`Tabs` root provider + `TabsList` + `TabsTrigger` + `TabsContent`, plus `TabsAddAction` only on the add-action variant), NOT a styled row of buttons: `role="tablist"` (with `aria-orientation`), native `<button role="tab">` triggers (`aria-selected`/`aria-controls`, roving `tabIndex`, automatic activation), `role="tabpanel"` (labelled back by its trigger id, `tabIndex={0}`). Keyboard: ArrowLeft/Right (horizontal), ArrowUp/Down (vertical), Home/End, wrap-around, disabled tabs filtered out of the key nav AND the tab order via native `disabled`. Controlled (`value`/`onValueChange`) AND uncontrolled (`defaultValue`) selection via a `TabsContext`; internal state is only created when uncontrolled. Panels ALL stay mounted and toggle with the `hidden` attribute (panel form/scroll state survives tab switches — documented in the README). Shared visual system (established by the `tabs` reference, honored in every variant): h-9 (36px) height matching the buttons family, `radius-sm` (underline = square + `-mb-px` + shared list border with a restrained 2px `color.primary` active rule; contained = `radius-md` bordered `surface-subtle` container with the selected tab lifting onto `surface` + `shadow-xs`), `text-sm leading-5 font-medium` labels, idle `muted-foreground` with `surface-hover` shift, selected `surface-active` + foreground text (not color alone), `focus-visible:outline-2 outline-offset-2 color.focus-ring`, `disabled:opacity-50` + `pointer-events-none`, `transition-colors duration-150 ease-out` + `motion-reduce:transition-none`, optional `icon` (ReactNode, `aria-hidden`, `[&_svg]:size-3.5`), `badge` (accent-soft chip, "New"/"Beta"), and `count` (bordered pill, `tabular-nums`) content props on every trigger. Tabs (11): tabs (reference), tabs-with-icons, tabs-with-badge, tabs-with-count, tabs-underline, tabs-contained, tabs-vertical (orientation="vertical" default, ArrowUp/Down, stacks list above panel below `sm`, `sm:w-56` list), tabs-scrollable (`overflow-x-auto` + `[scrollbar-width:none]`/`[&::-webkit-scrollbar]:hidden`, keyboard focus scrolls into view), tabs-disabled (`disabled` prop → native disabled, skipped by keys, out of tab order), tabs-with-panel (rich mounted-panel composition), tabs-with-add-action (`TabsAddAction` real button OUTSIDE the tablist with REQUIRED typed `aria-label`). No component-specific CSS files; no div-based fake tabs; no purple/neon/gradient/glassmorphism/emoji. Generated by `python3 _gen_react_tabs.py` (registry in `_gen_react_tabs_registry.py`; reuses the buttons preview architecture — `TOKEN_BLOCK` + `TAILWIND_CONFIG` + `PREVIEW_CSS` + `ICON_JS` from `_gen_react_buttons.py`; OWN esbuild parity conversion because the buttons converter only restores a single export and Tabs needs ALL named exports + default preserved; esbuild in `/tmp/dsbuild` build-time-only, not committed). `--check` flags drift. Audited by `python3 _audit_react_tabs.py` (treatment-neutralized shared-core equality across all 11 variants + export parity + semantics + anti-AI). QA'd by `python3 scripts/_qa_react_tabs.py` (Playwright: roles + id associations, click/Arrow/Home/End, vertical ArrowUp/Down, disabled skips, roving tabIndex, controlled+uncontrolled, scroll-into-view, add-action appends+selects, focus-visible 2px ring, dark-mode token flip, reduced-motion guard, 0 horizontal overflow @ 375/768/1280, 0 console errors — ALL 111 CHECKS PASSED). TSX strict-checked with `tsc` (typescript in /tmp/dsbuild, `/tmp/dsbuild/src/tabs-all` slug-named copies, react-jsx + bundler resolution + @types/react 18). Gotcha repaired during QA: the preview `ds-stack` shell uses `align-items:flex-start` (shrink-to-fit children), so scrollable tabs overflowed the page at 375/768 — showcase wrappers use `w-full space-y-6` instead of `ds-stack`. metadata.json: `technology:"react"`, `type:"component"`, `category:"Tabs"`, `subcategory` ∈ {Core, Content, Layout, States, Composite}, `languages:["JSX","TSX"]`, `styling:"Tailwind CSS"`. React still NOT registered in `snippets-index.json` (follow-up; `scripts/validate.py` passes — it only scans Tailwind/Vanilla trees).
  - **React/Components/Breadcrumbs/ (8 breadcrumbs, 2026-08-22):** the next React component family after Buttons/Inputs/Selects/Checkboxes/Radios/Switches/Textareas/Tabs, following the exact same `{code.tsx, code.jsx, preview.html, metadata.json, README.md}` asset shape and TypeScript-first/Tailwind-first stack. Multi-export compound family like Tabs — SIX shared primitives: `Breadcrumbs` (root `<nav aria-label>`, provides the default `separator` ReactNode through a `BreadcrumbsContext`), `BreadcrumbList` (`<ol>`, `flex-wrap` + `min-w-0` items — wraps, never scrolls the page), `BreadcrumbItem` (`<li>`), `BreadcrumbLink` (REAL `<a href>` — normal browser navigation, optional `icon` ReactNode rendered aria-hidden), `BreadcrumbCurrent` (plain text `aria-current="page"`, never a link), `BreadcrumbSeparator` (`<li role="presentation" aria-hidden>` — children ?? context separator ?? default chevron svg). Shared visual system (established by the `breadcrumbs` reference, honored in every variant): `text-sm leading-5`, muted-foreground links with foreground+underline hover, current = foreground `font-medium` (weight + color, not color alone), 14px icons (`[&_svg]:size-3.5`, currentColor), `radius-xs` + `focus-visible:outline-2 outline-offset-2 color.focus-ring`, `transition-colors duration-150 ease-out` + `motion-reduce:transition-none`. Breadcrumbs (8): breadcrumbs (reference), breadcrumbs-with-home (icon+text home, icon aria-hidden; icon-only home link via real `aria-label="Home"`), breadcrumbs-with-icons (optional per-level `icon` on Link AND Current — not forced on every item), breadcrumbs-with-current (`BreadcrumbLink` gains `current?: boolean` → renders the non-navigable aria-current span instead of an anchor, so data-driven trails map every level through one component), breadcrumbs-with-dropdown (`BreadcrumbDropdown` — one level exposes sibling pages: real `<button aria-haspopup="menu" aria-expanded>` + `role="menu"` of real `<a role="menuitem">` links, chevron rotates 180° while open, ArrowDown/Up opens + focuses first/last, arrows/Home/End cycle, Escape closes + refocuses trigger, Tab closes, outside-mousedown closes, menu item `current?: boolean` → `aria-current="page"` + font-medium), breadcrumbs-collapsed (`BreadcrumbEllipsis` — Home / … / Components / Buttons; `…` disclosure button with accessible name (default "Show hidden breadcrumb levels") opens a menu of the hidden levels as REAL links in path order — nothing hidden with CSS alone), breadcrumbs-max-width (`max-w-[9rem] sm:max-w-[14rem]` + `truncate` baked into Link/Current, `title` auto-filled from string children — full text stays in the accessibility tree AND the tooltip), breadcrumbs-with-separator (`separator` ReactNode on `<Breadcrumbs>` restyles the whole trail via context; per-position override via `<BreadcrumbSeparator>` children). No component-specific CSS files; no fake links (no divs/spans with click handlers); no purple/neon/gradient/emoji. Generated by `python3 _gen_react_breadcrumbs.py` (registry in `_gen_react_breadcrumbs_registry.py`; reuses the tabs multi-export esbuild parity conversion + the buttons preview architecture — `TOKEN_BLOCK` + `TAILWIND_CONFIG` + `PREVIEW_CSS` from `_gen_react_buttons.py`, `ICON_JS` extended inline with home/folder/book/grid/package cases; esbuild in `/tmp/dsbuild` build-time-only, not committed). `--check` flags drift. QA'd by `python3 scripts/_qa_react_breadcrumbs.py` (Playwright + static: 5-file shape, metadata schema, no `any`, nav landmark + ol + real anchors + exactly one non-anchor aria-current per trail + aria-hidden separators on all 8, focus-visible 2px ring (style `auto` accepted, same as tabs QA), dark-mode token flip, reduced-motion `transition-property:none`, dropdown full keyboard/menu behavior + outside-click + current-in-menu, ellipsis disclosure exposes hidden levels in order, max-width truncation + title, custom + overridden separators, real hash navigation on link click, 0 horizontal overflow @ 375/768/1280, 0 console errors — ALL 182 CHECKS PASSED; preview hrefs are `#/...` hash links so demos navigate without reload, showcase shows the live hash). TSX strict-checked with `tsc` (typescript@7 in /tmp/dsbuild, `/tmp/dsbuild/src/breadcrumbs` slug-named copies, react-jsx + bundler resolution + @types/react 18); TSX↔JSX export + prop-name parity verified via the `_check_react_parity.py` signature algorithm (gotcha: match JSX exports via the trailing `export { ... }` list — esbuild strips the `export` keyword from function declarations). metadata.json: `technology:"react"`, `type:"component"`, `category:"Breadcrumbs"`, `subcategory` ∈ {Core, Content, States, Layout, Composite}, `languages:["JSX","TSX"]`, `styling:"Tailwind CSS"`. React still NOT registered in `snippets-index.json` (follow-up; `scripts/validate.py` passes — it only scans Tailwind/Vanilla trees).
  - **React/Components/Pagination/ (8 pagination variants, 2026-08-20):** the next React component family after Buttons/Inputs/Selects/Checkboxes/Radios/Switches/Textareas/Tabs/Breadcrumbs, following the exact same `{code.tsx, code.jsx, preview.html, metadata.json, README.md}` asset shape and TypeScript-first/Tailwind-first stack. Multi-export compound family like Tabs/Breadcrumbs — SEVEN shared primitives: `Pagination` (root `<nav aria-label="Pagination">`, owns the current page via `PaginationContext` — controlled `page`+`onPageChange` or uncontrolled `defaultPage`, clamps every target into `1…totalPages`, provides `totalPages`/`size`/`disabled`/`buildHref` through context), `PaginationContent` (`<ul>`, `flex-wrap gap-1` — wraps, never scrolls the page), `PaginationItem` (`<li>`), `PaginationLink` (numbered page control), `PaginationPrevious`/`PaginationNext` (chevron + visible text label, auto-disabled at boundaries), `PaginationEllipsis` (informational only: `aria-hidden` "…" glyph + sr-only "More pages", NEVER a button). Dual-mode controls: with `buildHref` (or explicit `href`) they render REAL `<a href>` anchors (normal browser navigation, no click handlers); without URLs they render `<button type="button">` calling `setPage`; disabled controls render as non-interactive `<span aria-disabled="true">` (out of tab order, same geometry, no layout shift). Accessible names: inactive pages "Go to page N", current page "Page N" + `aria-current="page"`. Shared visual system (established by the `pagination` reference, honored in every variant): 36px md / 32px sm / 44px lg control heights (`size` prop, min-w = height, `tabular-nums`), `radius-sm`, idle muted-foreground with `surface-hover` shift, active page = `border color.border` + `bg-surface` + `shadow-xs` (border+surface, not color alone), `focus-visible:outline-2 outline-offset-2 color.focus-ring`, `transition-colors duration-150` + `motion-reduce:transition-none`, 14px chevrons (`[&_svg]:size-3.5`). Pagination (8): pagination (reference — state-driven buttons demo + URL-based anchors demo with `buildHref` `#/components/page/N` + live hash), pagination-with-previous-next (adds `PaginationStatus` — "Page X of Y" in `aria-live="polite"`; linear reader demo), pagination-with-numbers (explicit one-link-per-page mapping for small counts), pagination-with-ellipsis (adds `getPaginationRange(current, total, siblingCount=1)` — first/last/current±siblings, "ellipsis" markers only when a range is actually hidden, all pages when `totalPages <= 2*siblingCount+5` — plus `PaginationPages` renderer; 50-page file browser + 4-page no-ellipsis demo), pagination-compact (default `size="sm"`, dense orders-table footer), pagination-large (default `size="lg"` 44px touch targets, case studies), pagination-with-page-size (adds `PaginationPageSize` — explicitly labeled native `<select>` OUTSIDE the `<nav>` landmark since page size is a filter not navigation; showcase resets to page 1 on size change), pagination-disabled (first-page/last-page boundary disabling, per-link `disabled`, root `disabled`). No component-specific CSS files; no div-based fake controls; no emoji. Variant code.tsx files are DERIVED from the authored reference `pagination/code.tsx` (identical shared core + variant additions); regenerate derived files with `code.jsx`/`preview.html`/`metadata.json`/`README.md` via `python3 _gen_react_pagination.py` (registry in `_gen_react_pagination_registry.py`; reuses the breadcrumbs multi-export esbuild parity conversion + the buttons preview architecture; ICON_JS reuses the breadcrumbs set + adds a "file" glyph; esbuild in `/tmp/dsbuild` build-time-only, not committed). `--check` flags drift. QA'd by `python3 scripts/_qa_react_pagination.py` (Playwright + static: 5-file shape, metadata schema, no `any`, TSX↔JSX export parity, nav landmark + exactly one aria-current per numbered nav, page-click moves aria-current + updates list, prev/next boundary disabling, ellipsis window math at pages 1/2/4/50 + no-ellipsis small dataset, ellipsis not focusable/not a control, 32/36/44px heights, page-size select outside nav + updates rows + resets page, disabled spans not activatable + Tab skips them, focus-visible outline, dark-mode token flip, reduced-motion transition-none, 0 horizontal overflow @ 375/768/1280, 0 console errors — ALL 475 CHECKS PASSED). TSX strict-checked with `tsc` (typescript in /tmp/dsbuild, `/tmp/dsbuild/src/pagination` slug-named copies, react-jsx + bundler resolution + @types/react 18). metadata.json: `technology:"react"`, `type:"component"`, `category:"Pagination"`, `subcategory` ∈ {Core, Navigation, Layout, Density, Composite, States}, `languages:["JSX","TSX"]`, `styling:"Tailwind CSS"`. React still NOT registered in `snippets-index.json` (follow-up; `scripts/validate.py` passes — it only scans Tailwind/Vanilla trees).
  - **React/Components/Dropdowns/ (8 dropdown-menu variants, 2026-08-20):** the next React component family after Buttons/Inputs/Selects/Checkboxes/Radios/Switches/Textareas/Tabs/Breadcrumbs/Pagination, following the exact same `{code.tsx, code.jsx, preview.html, metadata.json, README.md}` asset shape and TypeScript-first/Tailwind-first stack. Multi-export compound family like Tabs/Breadcrumbs/Pagination — SEVEN core primitives: `DropdownMenu` (root provider: controlled `open`+`onOpenChange` or uncontrolled `defaultOpen`, `placement` bottom-start/bottom-end/top-start/top-end, outside-pointerdown close, `relative inline-flex` wrapper), `DropdownMenuTrigger` (real `<button>` `aria-haspopup="menu"`+`aria-expanded`, chevron rotate), `DropdownMenuContent` (`role="menu"` labelled by trigger, rendered only while open, layout-effect placement flip measured pre-paint with an `invisible`-until-measured guard), `DropdownMenuItem` (real `<button role="menuitem">` with `icon`/`shortcut`/`destructive`/`disabled`/`closeOnSelect`/`onSelect` props), `DropdownMenuLabel`, `DropdownMenuGroup` (`role="group"` + `aria-labelledby` wiring), `DropdownMenuSeparator` (`role="separator"`). Variant additions: `DropdownMenuCheckboxItem` (`role="menuitemcheckbox"` + `aria-checked`, `checked`/`defaultChecked`/`onCheckedChange`, stays open by default), `DropdownMenuRadioGroup`+`DropdownMenuRadioItem` (`role="menuitemradio"` + `aria-checked`, `value`/`defaultValue`/`onValueChange`), `DropdownMenuSub`+`DropdownMenuSubTrigger`+`DropdownMenuSubContent` (nested level: ArrowRight opens + focuses first item, ArrowLeft/Escape collapse only the level + refocus the sub trigger, hover opens without focus theft, sibling pointer-close via a per-level sub registry with a stable close callback, side flip right↔left). Real DOM focus (arrow keys move actual focus; item `closest('[role="menu"]')` scoping skips nested items) — no aria-activedescendant. Focus is never stranded: sub-close refocuses the sub trigger, tree-close refocuses the root trigger, Tab closes without refocus. Shared visual system (established by the `dropdown-menu` reference): 36px bordered trigger with `shadow-xs`, `radius-md` elevated panel (`surface-elevated` + 1px `color.border` + `shadow-md`), 13px items, item tone split (`ITEM_CLASSES` layout/interaction + `ITEM_TONE_DEFAULT`/`ITEM_TONE_DESTRUCTIVE` — the tone split exists because two conflicting text-color utilities let the later source-order class win, which broke destructive before the split), 16px icon slot (`[&_svg]:size-4`, muted or current/destructive), `ml-auto pl-6` shortcut column (aria-hidden + `aria-keyshortcuts`), uppercase/tracked labels. Dropdowns (8): dropdown-menu (reference — project actions, controlled open demo, placement demo), dropdown-menu-with-icons (document/account actions), dropdown-menu-with-sections (account/workspace groups), dropdown-menu-with-shortcuts (editor file menu, long-label truncation), dropdown-menu-destructive (repository danger zone + disabled-destructive member demo), dropdown-menu-checkboxes (view options + controlled external-sync demo), dropdown-menu-radio (theme controlled + sort uncontrolled), dropdown-menu-submenu (file share/move submenus + edge flip demo). No component-specific CSS files; no div-based fake items; no emoji. Variant code.tsx files DERIVED from the authored reference `dropdown-menu/code.tsx` (identical shared core + variant additions); regenerate derived files with `code.jsx`/`preview.html`/`metadata.json`/`README.md` via `python3 _gen_react_dropdowns.py` (registry in `_gen_react_dropdowns_registry.py`; reuses the pagination/breadcrumbs multi-export esbuild parity conversion + the buttons preview architecture; ICON_JS extends the breadcrumbs set with users/mail/link/monitor/sun/moon/logout/file/eye glyphs; esbuild in `/tmp/dsbuild` build-time-only, not committed). `--check` flags drift. QA'd by `python3 scripts/_qa_react_dropdowns.py` (Playwright + static: 5-file shape, metadata schema, no `any`, TSX↔JSX export parity, treatment-neutralized shared-core equality, menu semantics + focus restore + ArrowUp-first/last initial focus, disabled-skip, Escape/Tab/outside-pointer close, checkbox toggle-stays-open + controlled external sync, radio exclusivity, submenu two-level keyboard model + sibling pointer-close + edge flip, aria-keyshortcuts alignment, destructive token color (sampled after the 150ms tone transition settles), focus-visible ring, dark-mode body+panel token flip, reduced-motion `transition-property:none`, menu-open viewport-in bounds @ 375–1280, 0 console errors — ALL 732 CHECKS PASSED). TSX strict-checked with `tsc` (typescript in /tmp/dsbuild, `/tmp/dsbuild/src/dropdowns` slug-named copies, react-jsx + bundler resolution + @types/react 18). metadata.json: `technology:"react"`, `type:"component"`, `category:"Dropdowns"`, `subcategory` ∈ {Core, Content, States, Selection, Composite}, `languages:["JSX","TSX"]`, `styling:"Tailwind CSS"`. React still NOT registered in `snippets-index.json` (follow-up; `scripts/validate.py` passes — it only scans Tailwind/Vanilla trees).
  - **React/Components/Inputs/ (30 inputs, 2026-08-18 preview-fix):** the Inputs family ships the same `{code.tsx, code.jsx, preview.html, metadata.json, README.md}` asset shape and the same TypeScript-first/Tailwind-first stack as Buttons, but had a broken `preview.html` pattern (a single `<script type="text/babel">` block using ES `import { useId, useState } from "react"` with no `data-presets="react"` and no `window` exposure — Babel standalone cannot run ES-module `import`, so every Inputs preview rendered blank). Fixed by rebuilding all 30 previews with the exact buttons preview architecture via **`_gen_react_inputs_preview.py`** (reuses the proven transform — `_tsx_to_babel_component` + `TOKEN_BLOCK` + `TAILWIND_CONFIG` + `PREVIEW_CSS` + `ICON_JS` — imported from `_gen_react_buttons.py`): each preview now loads Tailwind CDN + React 18 UMD + Babel standalone, inlines the actual `code.tsx` (auto-transformed: TS types stripped via esbuild, `import`/`export` removed, wrapped in an IIFE exposing the component on `window.<Name>`), splits into three `<script type="text/babel" data-presets="react">` blocks (shared Icon/hooks, the component, the per-component `Showcase`), and carries a persisted no-flash light/dark toggle + the shared showcase shell (topbar/crumbs/eyebrow/title/lede/footer). The authored `Showcase` JSX in each existing preview is preserved verbatim (extracted, not rewritten). Run `python3 _gen_react_inputs_preview.py` to rewrite all 30 previews; `python3 _gen_react_inputs_preview.py --check` to detect drift. esbuild (build-time-only, not committed) must be at `/tmp/dsbuild`, same as the buttons generator. `code.tsx`/`code.jsx`/`metadata.json` were NOT modified — only `preview.html` (the fix is preview-only). Verified: `scripts/validate.py` passes; browser QA confirms rendering + interactivity (text-input 4-state grid, password-toggle Show/Hide `useState` toggling, command-input keyboard-navigable listbox, input-with-validation live error state).
- **Every Tailwind item carries a `type` field** (`component` | `section` | `template`) in its `metadata.json` AND in `snippets-index.json` (family + variant level), so search/filtering can identify content type without guessing from the URL/path. The index also keeps the Capitalized `category` bucket (`Components`/`Sections`/`Templates`). Vanilla metadata has no `type` field (two-type layout; `category` suffices).
- **Tailwind Components** (13 families, 321 variants): Accordions (15), Buttons (54, 3-level), Cards (40), Dropdowns (30), Input (49), Modals (30), Navbar (15), Navigation (35), Progress (6), Tables (20), Tabs (15), Toasts (6), Tooltips (6). NOTE: Navbar stays in Components per the task's explicit component classification; Footer/FAQ/etc. are Sections.
- **Tailwind Sections** (16 families, 200 variants): the 10 generated section categories (404, Blog, Contact, FAQ, Footer, Logos, Newsletter, Stats, Team, Testimonials, each 15 design styles) + the 6 multi-concept section categories (ai-product 12, app-ui 6, developer 6, marketing 6, premium-visual 3, saas 17). All formerly under `Tailwind/Components/`, now under `Tailwind/Sections/`. **2026-08-15 quality pass**: fixed critical defects (Testimonials `>heart<` SVG literals + `%s` placeholders, command-palette mojibake, Stats/futuristic counter JS, broken hex gradients, index sync) + significant a11y/semantics (404 `<h1>`, FAQ anchors, self-contained code.html CSS, Newsletter labels, kanban keyboard DnD, invalid `ring-current/N`); removed `saas/three-tier-pricing/sg` as a near-duplicate of `pricing-table/sg`. See CHANGELOG.md.
- **2026-08-16 Tailwind Newsletter horizontal-padding fix**: the shared `form()` helper in `_gen/builders_newsletter.py` (plus the `gradient` concept's custom inline form) constrained the email input + subscribe button to `max-w-md` (448px), while the surrounding supporting subtext uses `max-w-2xl` (672px). This mismatch left the form (input/button/icon) visibly narrower than the rest of the section content — "pushed inward" with large unused horizontal space and an inconsistent left/right content boundary. Fix: changed the three newsletter `<form>` `max-w-md` → `max-w-2xl` so the form aligns with the supporting text width. Effects verified by Playwright measurement across all 15 variants × {375,768,1280}: centered concepts now center a 672px form aligned with the 672px subhead; split/footer/stat/benefits/startup concepts now fill their grid column (max-w-2xl ≥ column width, so `w-full` takes over). The `inline`/`compact` strip forms (`sm:w-64`/`sm:w-48`) were left untouched — they are intentional compact horizontal strips, not the constrained-card pattern. **Follow-up (same fix, completing the consistent-boundary requirement):** 4 centered-card concepts (centered/glass/gradient/premium) ALSO had a card-internal supporting `<p>` description capped at `max-w-md` (448px) — narrower than the now-672px form, leaving an inconsistent boundary WITHIN the card (description 448 vs form 672). Changed those 4 `<p>` `max-w-md` → `max-w-2xl` (builders_newsletter.py lines ~43/61/79/106). Regenerated only `Tailwind/Sections/Newsletter/{editorial,monochrome,startup-landing,gradient-mesh}/code.html`+`preview.html` (restored metadata.json/README.md). Verified: description width == form width at 375/768/1280 (672==672 desktop), 0 overflow. NO `max-w-md` remains on any Newsletter form or supporting description. Note on regen scope: running the full `_gen/generate` would overwrite ALL 165 section folders (including manually-patched ones like Testimonials editorial), so targeted regen used throwaway scripts importing `style_for`/`preview_shell`/`code_snippet`/`metadata`/`readme` from `_gen.generate` with `ci=9` (Newsletter's BUILDERS index).
- **2026-08-16 Audit of all other Tailwind section categories for the same defect (none found beyond Contact):** scanned every remaining generated category (404, Blog, FAQ, Footer, Logos, Stats, Team, Testimonials) + the 6 multi-concept categories (ai-product, app-ui, developer, marketing, premium-visual, saas) with a Playwright "narrowed content block" scanner (flag elements <60% of section inner width at 1280). Triage: all flags are INTENTIONAL design, not the form-padding defect — (a) 404: `max-w-md`/`max-w-lg`/`max-w-xl`/`max-w-2xl` on centered error-page descriptions/panels (no forms; narrow centered error text is standard); (b) FAQ "search" concept: `max-w-xl` (576) search input over `max-w-3xl` (768) results list — deliberate search-focal hierarchy; (c) Footer: `max-w-xs` brand-column blurbs + `max-w-sm` centered footer blurb + newsletter-concept form fills its grid column (no cap); (d) Team: `max-w-xs` featured card (intentional); (e) Navbar: `max-w-md` search input (intentional); (f) saas multi-concept: `max-w-sm` footer subscribe widget (`sm:justify-self-end`), `max-w-xl`/`max-w-2xl` centered panels — all intentional. Grep confirmed NO remaining `<form ... max-w-md>` anywhere in Tailwind/Sections. Conclusion: only Newsletter (forms + card descriptions) and Contact (minimal-form) had the excessive-horizontal-padding defect; both fixed.
- **2026-08-16 Tailwind Contact horizontal-padding fix (same defect, one variant)**: audited all 15 Contact variants with Playwright (form width vs supporting-subtext width vs section inner width). Only ONE variant had the Newsletter-style excessive-padding defect: `Tailwind/Sections/Contact/elegant-luxury/` = the "Minimal Contact Form" concept, whose `<form class="mx-auto max-w-md space-y-4">` capped the form at 448px while the supporting subtext uses `max-w-2xl` (672px) — form pushed inward, inconsistent boundary. Same fix: `max-w-md` → `max-w-2xl` in `_gen/builders_contact.py` `minimal-form` concept; regenerated only that variant's code.html+preview.html (restored metadata.json/README.md). Verified: form now 672px == subtext 672px at 768/1280 (335==335 at 375), 0 overflow at 375/768/1280. The other Contact concepts were NOT defective: no-form concepts (cards/team/support/offices/inline/onboarding) are fine; the split/map/startup/glass/mega/sidebar/newsletter-contact concepts place full-width `space-y-4` forms inside cards/grid-columns (no artificial cap); `edge-glassmorphism` ("Centered Contact" / dark concept) wraps the form in an intentional `mx-auto max-w-2xl` panel that already aligns with the subtext — left untouched. Contact BUILDERS index `ci=2`; minimal-form is concept `n=6` → `style_for(2,6)` = `elegant-luxury` (offset=(2*4)%15=8, CATEGORY_STYLES[(6+8)%15]=idx14).
- **2026-08-16 Tailwind Sections spacing/overlap QA**: ran an automated Playwright scanner across all 200 variants × 3 widths (375/768/1280) checking horizontal overflow, leaf-text overlap (via Range.getClientRects), viewport-edge text collision, and text clipping. Of 411 raw signals, 34 variants flagged; after triage most were intentional design (Logos/Testimonials marquees, Kanban horizontal scroll, Newsletter `sr-only` labels, saas `truncate` metadata, layered decorative cards). 4 real defects fixed: (1) `saas/trial-cta/neo-brutalism` headline `leading-[0.9]` with 72px uppercase → line overlap (bumped to `leading-[1.1]`); (2) `saas/launch-hero/neo-brutalism` headline `leading-[0.92]` with 88px uppercase → line overlap (bumped to `leading-[1.05]`); (3) `404/dark-premium` giant `text-9xl` "404" glyph collided with subtitle — note Tailwind named `text-*` sizes set `line-height` and `sm:text-9xl` overrides non-responsive `leading-*`, so the fix required `leading-[1.15] sm:leading-[1.4]`; (4) `Testimonials/editorial` (video concept) had a generator arg-swap bug in `_gen/builders_testimonials.py` where `b["badge"]` and `ic("play",...)` were swapped, producing the badge className as visible button text + the play SVG swallowed into the duration span's class attribute — fixed the generator arg order AND patched the committed output files. Gotcha for future scanning: `Range.getClientRects` on large display fonts returns the font's full em-box (ascent+descent ≈ 1.36× font-size), not the line-box, so `line-height: 1` on giant display numbers causes glyph overflow into the next sibling even when margins look adequate.
- **Tailwind Templates** (10): ai-saas-platform, atlas-analytics, baseline-conference, devsnips-store, krat-adventure, meridian, northline-atelier, quiet-place, stratum, vesper. Unchanged location (`Tailwind/Templates/`).
- The index regenerator (`_gen/rebuild_index.py`) scans each content-type tree on disk (`TAILWIND_TREES` = Components/Sections/Templates, `VANILLA_TREES` = Components/Templates). It preserves hand-curated family-level `description`/`tags`/`searchTerms` and variant-level fields by matching on normalized path (with a `Tailwind/Sections/`->`Tailwind/Components/` fallback for section families moved from Components). On-disk section families with no family-level metadata get curated names/descriptions from `SECTION_FAMILY_NAMES`/`SECTION_FAMILY_DESCRIPTIONS` maps. It cross-validates indexed == on-disk and refuses to write on mismatch. Adds `type` to every Tailwind family/variant. Computes `stats.tailwindByType`.
- The index `category` field uses Capitalized values: **Components / Sections / Templates**. Individual Tailwind `metadata.json` `category` was normalized to the Capitalized bucket name during the migration; `type` is the lowercase semantic enum.
- The section-style generator (`_gen/generate.py` + `_gen/update_index.py`) now writes to `Tailwind/Sections/` (not `Tailwind/Components/`) with `type:"section"`, `category:"Sections"`. Run `python3 -m _gen.generate` then `python3 -m _gen.update_index` (or the full regenerator `python3 -m _gen.rebuild_index`).
- **Site navigation (new):** `index.html` (repo root, tech landing) -> `Tailwind/index.html` (three discovery cards: Components/Sections/Templates) -> `Tailwind/Components/index.html`, `Tailwind/Sections/index.html`, `Tailwind/Templates/index.html` (category indexes that fetch `../../snippets-index.json` and render family cards filtered by `type`). Consistent breadcrumbs (`DevSnips / Tailwind / <Type>`), sticky primary nav (Overview/Components/Sections/Templates), dark-mode toggle (persisted, no-flash), responsive 375-1280px, `prefers-reduced-motion` safe, skip link, `:focus-visible` rings. Validated by `scripts/test_tailwind_nav.py` (Playwright).
- Removed: `Vanilla/Utilities/` (76 snippets), `Vanilla/Resources/` (67 JS helpers), and empty reserved dirs (`Tailwind/Utilities/`, `Tailwind/Pages/`, `Vanilla/Tools/`). No existing component depended on them.
- `Tailwind/Components/STYLE_TOKENS.md` is the design-token reference for the section-style components (now in Sections/).
## What this repo is
DevSnips is an open-source, framework-free frontend component library organized as design-system "families". Each Tailwind family lives under `Tailwind/Components/` (e.g. `Tailwind/Components/Accordions/`, `Tailwind/Components/Tables/`) and contains variant sub-folders. Vanilla families live under `Vanilla/Components/`. The two content types are **Components** (reusable UI building blocks and sections) and **Templates** (complete website/page templates).

## Folder + file convention (per variant)
Every Tailwind variant folder (kebab-case) must contain exactly three files:
- `code.html` ‚Äî component ONLY. No `<html>`/`<head>`/`<body>`/`<!doctype>`/Tailwind CDN. Copy-paste ready.
- `preview.html` ‚Äî full `<!DOCTYPE html>` page with Tailwind CDN (`https://cdn.tailwindcss.com`), Inter font, responsive layout, and realistic application context around the component.
- `metadata.json` ‚Äî see schema below.

Vanilla variant folders contain a self-contained `component.html` (inline `<style>`+`<script>`), `metadata.json`, and `README.md`. The migrated Neo-Brutalist sections ship as full `<!DOCTYPE html>` pages; the original legacy components ship as copy-paste-ready snippet fragments (no `<!DOCTYPE>` wrapper). The directory name remains the canonical component id/slug; the single primary component source file is always named `component.html`.

The `code.html` snippet comment header is optional but follows CONTRIBUTING.md:
`<!-- Snippet Name / Description / Author: DevSnips Contributors / Usage Example -->`

## metadata.json schema (used across Tables, Cards, Accordions)
```json
{
  "name": "Display Name",
  "slug": "kebab-folder-name",
  "component": "accordion",        // singular family noun
  "family": "accordions",          // plural
  "variant": "basic",              // short variant key
  "description": "...",
  "framework": "Tailwind CSS",
  "language": "HTML",
  "tags": ["..."],
  "related": ["other-variant-slug"],
  "features": ["..."]
}
```
Required keys: name, slug, component, family, variant, description, framework, language, tags, related, features. `slug` must equal the folder name.

## snippets-index.json registration
- Top-level `families[]` array; each family has `name`, `path`, `tech`, `category`, `description`, `variantsCount`, `variants[]` (each with name/path/description/features/tags/files), `tags`, `searchTerms`.
- Update `stats.totalFamilies` and `stats.totalVariants` (sum of variantsCount) after adding a family.
- Also add the family name to `technologies[].families` for the matching tech (`Tailwind CSS`).

## Accordion JS pattern (verified working)
Use a `<div data-accordion="name">` wrapper containing `<div data-accordion-item>` blocks and an inline `<script>` at the end. The script scopes itself with:
```js
const root = document.currentScript.closest('[data-accordion]');
```
This works because the `<script>` parses inside the root. Panel animation uses the CSS-grid trick:
`grid grid-rows-[0fr]` ‚Üî toggle `grid-rows-[1fr]` with `transition-[grid-template-rows] duration-300 ease-out`, wrapped in `overflow-hidden`. Chevron rotates via `style.transform = 'rotate(180deg)'`. Single-open mode: add `data-single-open` attr and close siblings on open. Always set `aria-expanded` + `aria-controls` + `role="region"` + `aria-labelledby` + `focus-visible:ring`.

## Code standards
- HTML + Tailwind CSS only. Vanilla JS only where interaction is required.
- NO React/Vue/Alpine/Bootstrap/jQuery.
- 2-space indentation. Semantic HTML. Accessibility required (ARIA, keyboard, focus rings).

## Vanilla design tokens ‚Äî "Swiss" (neo-minimal, industry-standard)
A single canonical token system makes the 201 legacy components speak one visual language. Source of truth: `Vanilla/Components/tokens.css` + `Vanilla/Components/DESIGN_TOKENS.md`.
- **Token vocabulary** (`--ds-*`): neutrals (stone ramp), one accent (blue-600), semantic status (WCAG AA), typography (system stack + size/weight/leading ramps), spacing (base-4, Tailwind-aligned), radius (sm/md/lg/xl/full), shadow (restrained, Swiss ‚Äî not the brutalist offset), motion, layout. Light + dark via `prefers-color-scheme`.
- **Components opt in** with `var(--ds-<token>, <original-value>)` ‚Äî the original value is the fallback, so a component is **copy-paste standalone** AND renders identically until `tokens.css` is themed. Including `tokens.css` once upgrades every component together (re-theme by editing one file). This is the shadcn model (CSS variables + per-theme override).
- **Migration** (`_gen/migrate_tokens.py`, deterministic + idempotent): replaces ad-hoc hex/radius/shadow/font values with `var(--ds-*, fallback)`, injects the **full** Swiss `:root{--ds-*}` block + dark-mode `@media (prefers-color-scheme: dark)` override into each component's `<style>` so every `var(--ds-*)` reference resolves to the Swiss value. `--refresh-blocks` re-injects the current block (brace-matched replacement) without re-running value replacement ‚Äî use it after editing the token set. Result: **201/201 legacy components tokenized (was 1.5%), 654 `var(--ds-*)` references, all 24 referenced tokens resolve to Swiss values, only 56 genuinely raw hex remaining** (component-specific gradient/decorative colors). The 65 neo-brutalist migrated sections keep their own `--bg`/`--surface`/`--radius` system (a deliberate, already-cohesive design language) and are NOT touched.
- **Conformance** (`scripts/qa_vanilla.py --tokens`): reports adoption ‚Äî counts `var(--ds-*)` references vs raw hex (excluding fallbacks + token definitions) per component; lists the top offenders. Advisory (always exits 0).

## Vanilla quality bar (enforced)
`scripts/qa_vanilla.py` scans every Vanilla component against an enforceable quality bar and is wired into `scripts/validate.py` (a required-check failure fails validation, exit 1). Run standalone for the full report (`--only-failures` for just failures; `--json` for machine-readable).
- **reduced-motion** (required on animated components): every component with `transition`/`animation`/`@keyframes` must guard them with a `@media (prefers-reduced-motion: reduce)` rule. `_gen/fix_quality_bar.py` injects a global guard idempotently (marker `/* devsnips-qa:quality-bar */`).
- **focus-visible** (required): every component must have a `:focus-visible` ring so keyboard users see focus. `fix_quality_bar.py` injects `:focus-visible{outline:2px solid #2563eb;outline-offset:2px;}`.
- **aria/role** (required on interactive families: Modals, Dropdowns, Tabs, Accordions, Navigation, Tooltips, Loaders, Buttons, Forms): custom widgets must use `role`/`aria-*`. Native semantic elements (`<button>`, `<a href>`, `<input>`, `<select>`, `<textarea>`, `<summary>`, `<dialog>`, `<details>`, `<progress>`, `<meter>`, `<output>`; content semantics `<kbd>`/`<time>`/`<mark>`/`<abbr>`; structure `<p>`/`<h1-6>`/`<ul>`/`<ol>`/landmarks) satisfy this by themselves ‚Äî W3C: prefer native semantics, "no ARIA is better than bad ARIA".
- **keyboard** (required only when the component wires interaction via `onclick`/click-listeners/div-based controls): operable controls must be `<button>`/`<a>`/`<input>` or carry `tabindex`+`role="button"`+Enter/Space handlers. Pure status displays (spinners, progress bars, skeletons) need aria but not keyboard.
- Informational (non-fatal) warns: `doctype` (legacy fragments ship without `<!DOCTYPE>` ‚Äî acceptable), `lang`/`viewport` (only when DOCTYPE present), `semantic` landmarks.

## Tailwind SaaS Sections ‚Äî `Tailwind/Components/saas/` (merged ‚Äî formerly `Tailwind/Sections/saas/`)
> **SUPERSEDED (2026-08-13):** Tailwind Sections are now a first-class content type under `Tailwind/Sections/`, NOT merged into `Tailwind/Components/`. See the CURRENT AUTHORITATIVE STATE block at the top of this file. The detail below is retained for historical context.
Premium SaaS website sections (one variant/style per section, mixed across the three design styles). Same 3-file convention as other Sections families (`code.html` / `preview.html` / `metadata.json`).
15 sections shipped: product-hero (vercel), launch-hero (neo-brutalism), dashboard-hero (sharp-glassmorphism), feature-grid (neo-brutalism), bento-showcase (sharp-glassmorphism), product-workflow (vercel), usage-pricing (neo-brutalism), pricing-comparison (vercel), pricing-table (neo-brutalism/sharp-glassmorphism/vercel), logo-cloud (vercel), testimonials (sharp-glassmorphism), metrics (neo-brutalism), screenshot-showcase (sharp-glassmorphism), trial-cta (neo-brutalism), enterprise-footer (vercel). (three-tier-pricing was removed 2026-08-15 as a near-duplicate of pricing-table.) Several include scoped vanilla-JS interactivity (countdown, billing toggle, usage calculator, workflow step switcher, screenshot tabs, count-up, newsletter) using the `document.currentScript.closest('[data-<scope>="<style>"]')` pattern. Registered in `snippets-index.json` under `tech: "Tailwind CSS"`, `category: "Sections"`.

## Tailwind Sections ‚Äî `Tailwind/Components/` (merged ‚Äî formerly `Tailwind/Sections/`)
Multi-style website sections organized as `category/section/style/` (three levels, all kebab-case). Each style folder contains exactly: `preview.html` (full `<!DOCTYPE html>` page with Tailwind CDN + app-context shell), `code.html` (snippet only ‚Äî no DOCTYPE/CDN), `metadata.json` (keys: name, slug, category, subcategory, section, style, description, framework, language, tags, features, responsive, dependencies). `slug` = `<section>-<style>`.

Three shared design styles with distinct token palettes (canonical reference in `Tailwind/Components/STYLE_TOKENS.md`):
- `neo-brutalism` ‚Äî Archivo + JetBrains Mono; hard `border-2 border-black`, offset `shadow-[8px_8px_0_0_#000]`, flat bright accents (#FFE600/#FF4FA3/#00E676/#00C2FF), press-down hover, cream `#FFFDF5` bg. Scope attrs use `="nb"`.
- `vercel` ‚Äî Geist + Geist Mono; dark `#050505`/`#0a0a0a`, `border-white/10` hairlines, single teal `#50e3c2` accent, white primary buttons. Scope attrs use `="vc"`.
- `sharp-glassmorphism` ‚Äî Sora + JetBrains Mono; `bg-white/10 backdrop-blur-2xl` glass over animated `.sg-mesh` gradient (fuchsia/indigo/cyan), gradient CTAs, cyan `#6ee7ff` glow. Scope attrs use `="sg"`. Glass needs a colored backdrop to read.

JS scoped via `document.currentScript.closest('[data-<thing>="<style>"]')` so snippets work standalone. Categories: ai-product (ai-chat-interface, model-comparison, prompt-library, agent-workflow), saas, developer, app-ui, marketing, premium-visual. Registered in `snippets-index.json` `families[]` with `tech: "Tailwind CSS"`, `category: "Components"`, path = `Tailwind/Components/<category>/<section>/`; variants are the style folders. Also listed under `technologies[].families` for Tailwind CSS.

## Vanilla Sections (Neo-Brutalist) ‚Äî `Vanilla/Components/` (merged ‚Äî formerly `Vanilla/Sections/`)
- 65 self-contained website sections across 16 families (Hero, Navigation, Features, Logos, Statistics, Products, Pricing, Testimonials, Team, Process, Content, Gallery, FAQ, CTA, Contact, Footer).
- Folder = `Vanilla/Components/<Family>/<kebab-slug>/` containing exactly: `component.html` (self-contained: inline `<style>` + `<script>`, full `<!DOCTYPE html>`, body class `nb`), `metadata.json`, `README.md`. This matches the existing Vanilla component convention (one `component.html` per variant), NOT the Tailwind code.html/preview.html split.
- Shared design tokens embedded in each `component.html` `<style>` `:root`: `--bg --surface --foreground --muted --border --primary --accent --pink --lime --cyan --radius --shadow --shadow-lg --ring --container --gutter`. Light + dark via `prefers-color-scheme`. Reduced-motion safe.
- `metadata.json` keys: id, name, slug, component, family, variant, description, framework, language, technology, category, subcategory, tags, features, responsive, darkMode, accessibility, browserSupport, dependencies, source, related.
- Browse via `Vanilla/Components/sections-index.html` (filterable gallery) and `Vanilla/Components/sections-showcase.html` (all sections live, each in an isolated iframe).
- Registered in `snippets-index.json` `families[]` with `tech: "Vanilla HTML/CSS/JS"`, `category: "Components"`; also listed under `technologies[].families` for the Vanilla tech.

## Tailwind Section-Style Components (15-Style Multi-Concept) ‚Äî `Tailwind/Components/<Category>/<style-slug>/`
> **SUPERSEDED (2026-08-13):** Tailwind Sections are now a first-class content type under `Tailwind/Sections/`, NOT merged into `Tailwind/Components/`. See the CURRENT AUTHORITATIVE STATE block at the top of this file. The detail below is retained for historical context.
- 11 section categories (Testimonials, FAQ, Contact, Footer, Navbar, Stats, Team, Blog, Logos, Newsletter, 404), each with 15 variant folders = 165 section-style components, 660 files total.
- Folder = `Tailwind/Components/<Category>/<style-slug>/` (merged from `Tailwind/Sections/` in the 2026-08-10 architecture migration) ‚Äî the folder is named after its **design style** (not `Section-NN`), matching the repo's existing multi-style convention (`developer/code-playground/neo-brutalism/`, etc.). Each category uses each of the 15 styles exactly once (1:1 permutation), so the folder name is the style slug. Each folder contains exactly 4 files: `preview.html` (full `<!DOCTYPE>` + Tailwind CDN + Google Fonts + style head_css + body decor), `code.html` (snippet only ‚Äî snippet comment header + `<section>`, NO DOCTYPE/CDN), `metadata.json` (keys: id=`<category>-<style-slug>`, slug=id, name, technology=tailwind, category=components, subcategory, section, style, description, framework, language, tags, features, responsive, darkMode, accessibility, browserSupport, dependencies), `README.md` (features + responsive + browser support + usage + design language).
- 15 design styles defined in `_gen/styles.py` TOKENS dict: neo-brutalism, edge-glassmorphism, vercel, minimal, apple-inspired, bento-grid, editorial, dark-premium, startup-landing, futuristic, gradient-mesh, soft-ui, cyber, monochrome, elegant-luxury. Each has: title, fonts, font_url, font_display, font_mono, head_css (CSS vars + .f-disp/.f-mono/.nb-shadow/.sg-mesh helpers + prefers-color-scheme), body_class, decor (fixed animated background), surface/surface_soft/badge/btn_primary/btn_secondary/input/chip/hover_card/text_muted/accent/accent2/text tokens.
- Style rotation per category is offset (`offset = (cat_index*4) % 15`) so each category spans all 15 styles with even distribution (11 sections per style across the library).
- Generator lives in `_gen/`: `styles.py` (tokens), `helpers.py` (esc/fill/avatar/ic/star_row/logo_svg/ICONS), `layout.py` (head/section wrappers), `builders_<category>.py` (15 concepts each), `generate.py` (writes files into `Tailwind/Components/`), `update_index.py` (updates snippets-index.json). Run `python3 -m _gen.generate` then `python3 -m _gen.update_index`. The full-index regenerator is `_gen/rebuild_index.py` (`python3 -m _gen.rebuild_index`).
- Registered in `snippets-index.json` as 11 families named `<Category> (Tailwind)` with `tech: "Tailwind CSS"`, `category: "Components"`, path `Tailwind/Components/<Category>/`, variantsCount=15. Also listed under `technologies[].families` for Tailwind CSS.
- Pure HTML + Tailwind CSS only (vanilla JS only for navbar/footer interactivity via scoped inline scripts). No React/Vue/Alpine/Bootstrap/DaisyUI/Flowbite. Inline `<style>` only for pure-CSS animations (logos marquee, 404 blinking cursor).


## Tailwind Templates ‚Äî `Tailwind/Templates/<slug>/`
Full website templates (not single components). Two scopes exist:
- **Multi-page** (`type: "multipage"`): `ai-saas-platform` (11 pages incl. components/ + assets/), `baseline-conference` (6 pages), `northline-atelier` (4 pages), `devsnips-store` (7 pages incl. about + assets/). Pages live in `pages/`, shared navbar/footer in `components/` (ai-saas only) ‚Äî otherwise navbar/footer are inlined per page (copy-paste, NOT server includes).
- **Single-page** (`type: "single-page"`): `quiet-place`, `krat-adventure` (root `index.html`), `stratum` (`pages/index.html` + `preview.html`), `meridian` (`pages/index.html` + `preview.html`).

### `devsnips-store` ‚Äî Editorial Modern + Monochrome Luxury ecommerce (multipage)
- Fictional brand: **DevSnips** ‚Äî a curated store of considered design objects (vessels, lighting, textiles, furniture). Files: `preview.html` (template gallery shell), `metadata.json`, `README.md`, `assets/icons/logo.svg` (a hairline **[D] mark** representing DevSnips ‚Äî bracket-serif D in a framed square, single clay dot), `assets/images/og-image.svg`, `pages/{index,shop,product,cart,checkout,journal,about}.html` (7 pages).
- Design system tokens (defined in `tailwind.config` colors in each page head): ink `#14110D`, paper `#F5F1EA`, bone `#ECE6DB`, cream `#FBF8F1`, stone `#8A8275`, clay `#B5552D` (single warm accent). Fonts: Fraunces (display serif, optical sizing), Inter (body), JetBrains Mono (labels/prices/metadata). Geometry: 1px hairline rules (`rgba(20,17,13,0.16)` via `.rule`/`.rule-strong`), square-ish radii, **no drop shadows or gradients**, generous whitespace. Oversized editorial headlines via `clamp()`, numbered sections, magazine masthead rhythm. Deliberately differs from stratum/meridian: warm neutrals + single clay accent + Fraunces serif instead of lime/amber + Archivo/Space Grotesk; hairlines instead of offset shadows.
- 7 pages: Home (editorial issue-cover hero with masthead bar + oversized serif headline + numbered collection index + featured object panel; new arrivals grid with hover-reveal strips; material-spectrum swatches; featured collection; journal preview; dark newsletter), Shop (category filter chips + sort + live count, scoped JS), Product (thumbnail gallery + finish swatches + quantity stepper + single-open care-guide accordion CSS-grid `0fr->1fr` + related), Cart (working quantity steppers + remove + receipt-style order summary + shipping estimate + empty state, scoped JS), Checkout (3-step stepper + contact/shipping/payment + fixed order summary), Journal (featured entry + tag filters + magazine cards), About/Atelier (principles + process timeline + maker studio cards).
- Shared navbar: centered [D] logo + wordmark, left nav (mobile hamburger below `lg`), right Search/Account/Cart with clay badge count. Shared footer: 4-col + social. Scoped vanilla JS (IIFEs): mobile menu, filters, gallery, swatches, quantity, accordion, cart math, newsletter, IntersectionObserver scroll reveal. All reduced-motion safe.
- QA verified: strict HTML5 valid (html5lib), valid metadata.json, 0 horizontal overflow at 320/375/390/430/768/1024/1280/1440/1920px (Playwright, all 8 files √ó 9 widths), 0 console errors, all interactions pass. Note: cart line-item flex rows need `min-w-0` on the flex-1 content column and `flex-wrap` on the bottom stepper/remove row to avoid 320px overflow.
- Registered in `snippets-index.json` as family `DevSnips Curated Design Store (Template)` (`category: "Templates"`, `tech: "Tailwind CSS"`, `variantsCount: 1`); also under `technologies[Tailwind CSS].families`. Stats after: totalFamilies=90, totalVariants=735.

### `stratum` ‚Äî Neo-Brutalism + Editorial Modern fintech ops (single-page)
- Fictional brand: **Stratum** ‚Äî vertical fintech ops platform (treasury / bill pay / spend control). Files: `pages/index.html` (full landing, 11 composed sections), `preview.html` (template gallery shell ‚Äî overview + sections index + design-system summary), `assets/icons/logo.svg`, `assets/images/og-image.svg`, `metadata.json`, `README.md`.
- Design system tokens (defined in `tailwind.config` colors in the page head): ink `#0A0A0A`, paper `#FBFAF7`, cream `#F4F1EA`, lime `#C6F24E` (primary accent), ember `#FF4FA3`, sky `#00C2FF`, forest `#007A52` (success ‚Äî darkened from `#00A86B` for WCAG AA on light bg). Fonts: Archivo (display 500-900), Inter (body), JetBrains Mono (labels/metadata). Geometry: 2px borders, square radii, offset shadows `shadow-brutal` 8px / `shadow-brutal-sm` 4px / `shadow-brutal-lg` 12px, `.press` translate-hover interaction.
- Hero = editorial split: copy left + hand-built "treasury command surface" product panel right (live status header, balance, animated SVG sparkline with stroke-dash draw, account ledger rows, sync footer) flanked by two floating mini-cards (approval needed / FX hedge, `sm:block` desktop-only). Bento capabilities section includes a policy-as-code approval-routing graph. Workflow = 5-step capture->reconcile with per-step timings. Pricing = cash-managed tiers (not per-seat). FAQ = scoped single-open accordion (CSS-grid `0fr->1fr`, `aria-expanded`/`aria-controls`/`role=region`, chevron rotate). CTA = dark panel with corner-tick framing.
- Scoped vanilla JS (inline `<script>` at body end): mobile menu toggle (aria-expanded + icon swap + auto-close on link click), FAQ accordion single-open. CSS-only: `.press` hover, `.link-underline` grow, `.live-dot` pulse, `.marquee-track` logos, `.spark-path` draw.
- **Accessibility**: skip link, semantic landmarks, single h1, AA-normal contrast on all text (opacity text uses `/60` minimum ‚Äî `/40`/`/50` fail; forest token darkened to `#007A52`). `prefers-reduced-motion` disables marquee/sparkline/transitions.
- QA verified: 0 horizontal overflow at 320/375/390/430/768/1024/1280/1440/1920px, 0 console errors, accordion + mobile menu interactions pass, strict HTML5 valid (html5lib), metadata.json valid.
- Registered in `snippets-index.json` as family `Stratum Fintech Ops Platform (Template)` (`category: "Templates"`, `tech: "Tailwind CSS"`, `variantsCount: 1`); also under `technologies[Tailwind CSS].families`. Stats after: totalFamilies=88, totalVariants=733.

### `meridian` ‚Äî Neo-Industrial + Swiss Minimal incident command (single-page)
- Fictional brand: **Meridian** ‚Äî incident command platform for on-call engineering teams (alert routing / incident command / postmortems). Files: `pages/index.html` (full landing, 11 composed sections), `preview.html` (template gallery shell ‚Äî overview + sections index + design-system summary), `assets/icons/logo.svg`, `assets/images/og-image.svg`, `metadata.json`, `README.md`.
- Design system tokens (defined in `tailwind.config` colors in the page head): canvas `#F5F6F8` (cool engineering paper), ink `#0B0D10`, surface `#FFFFFF`, panel `#EDEFF3`, muted `#5C6470`, amber `#E8A33C` (single primary accent), ok `#16A34A` + alert `#DC2626` (status dots/badges only). Fonts: Space Grotesk (display 400-700), Inter (body), JetBrains Mono (labels/metadata). Geometry: 1px hairline rules (`hl`/`hl-strong` utilities, `rgba(11,13,16,0.14)`), square radii, restrained tonal elevation (no offset shadows), graph-paper `.blueprint`/`.blueprint-fine` grid backgrounds. Deliberately differs from `stratum`: hairlines + tonal lift instead of 2px borders + offset shadows; Space Grotesk + amber on cool canvas instead of Archivo + lime on cream.
- Hero = editorial-technical split: copy left + hand-built "incident command surface" product panel right (dark title bar with live pulse dot, latency metric + animated SVG sparkline with stroke-dash draw + gradient fill, responders with avatar chips, timeline, runbook footer) flanked by two floating mini-cards (correlated signal / stepped escalation, `lg:block` desktop-only). Signature routing section = directed alert-routing graph (signal -> correlate node -> routing rule -> page owner, with dashed ack-fallback branch, animated `edge` flow). Workflow = 5-step incident lifecycle with per-step timings + signed-state rail. Pricing = per-on-call-team tiers (not per-seat). FAQ = scoped single-open accordion (CSS-grid `0fr->1fr`, `aria-expanded`/`aria-controls`/`role=region`/`aria-labelledby`, chevron rotate). CTA = dark panel with corner-tick framing + 3-step onboarding panel.
- Scoped vanilla JS (inline `<script>` at body end): mobile menu toggle (aria-expanded + icon swap + auto-close on link click), FAQ accordion single-open. CSS-only: hairline card hover (border-color + tonal lift), `.link-underline`/`.link-underline-amber` grow, `.live-dot`/`.live-dot-amber` pulse, `.marquee-track` logos, `.spark-path` draw, `.edge` routing flow.
- **Accessibility**: skip link, semantic landmarks, single h1, ARIA on accordion (labelledby + controls + region), mobile menu aria-expanded, SVG diagrams `role="img"` + `aria-label`, decorative SVGs `aria-hidden`. `prefers-reduced-motion` disables marquee/sparkline/edge-flow/transitions.
- QA verified: 0 horizontal overflow at 320/375/390/430/768/1024/1280/1440/1920px, 0 console errors (only Tailwind CDN production warning), accordion single-open + mobile menu interactions pass, strict HTML5 valid (html5lib), metadata.json valid.
- Registered in `snippets-index.json` as family `Meridian Incident Command Platform (Template)` (`category: "Templates"`, `tech: "Tailwind CSS"`, `variantsCount: 1`); also under `technologies[Tailwind CSS].families`. Stats after: totalFamilies=89, totalVariants=734.

### `vesper` ‚Äî Glassmorphism + Cyber Minimal attack-surface management (single-page)
- Fictional brand: **Vesper** ‚Äî external attack-surface management (EASM) platform for security teams (discover exposed assets / prioritize real exposures / verify remediation). Files: `pages/index.html` (full landing, 11 composed sections), `preview.html` (template gallery shell ‚Äî overview + sections index + design-system summary), `assets/icons/logo.svg`, `assets/images/og-image.svg`, `metadata.json`, `README.md`.
- Design system tokens (defined in `tailwind.config` colors in the page head): canvas `#06070D` (space black), ink `#E6E8EC`, surface `#0C0E17`, panel `#11131F`, muted `#8A90A2`, cyan `#6EE7FF` (primary accent / signal), amber `#FBBF24` (high severity only), red `#F43F5E` (critical only), ok `#34D399` (resolved only). Fonts: Sora (display 400-800), Inter (body), JetBrains Mono (labels/metadata). Geometry: `rounded-2xl` frosted-glass surfaces (`backdrop-blur(20px)`), 1px white/10 hairlines, restrained cyan glow (drop-shadow on active states only), no offset hard shadows. Atmospheric backdrop: low-opacity fuchsia `#D946EF`/indigo `#6366F1`/cyan `#6EE7FF` mesh (slow-drift) + fine `cyber-grid` + radial vignette ‚Äî a backdrop, not the gradient-blob anti-pattern. Deliberately differs from `stratum` (light, 2px borders + offset shadows) and `meridian` (light, 1px hairlines + tonal lift).
- Hero = editorial-cyber split: copy left + hand-built "attack-surface command console" product panel right (title bar with severity dots + SCANNING live status, tracked-assets/open-exposures summary with severity bar + legend, exposure rows with CRIT/HIGH/MED pills, live surface radar SVG with rotating sweep + plotted severity assets, exposures-per-day sparkline with draw animation, verified footer rail) flanked by two floating mini-cards (critical exposure + new asset discovered, `lg:block` desktop-only). Signature topology section = directed asset-reachability graph (internet ‚Üí load balancer ‚Üí API gateway ‚Üí K8s API / Postgres / S3) with exposed attacker path animated in cyan and critical node ringed in red, + side exposure-detail card. Lifecycle = 5-step EASM workflow (Discover ‚Üí Classify ‚Üí Prioritize ‚Üí Remediate ‚Üí Verify) with per-step timings + signed-state rail (SHA-256 tamper-evident). Pricing = per-asset tiers (not per-seat). FAQ = scoped single-open accordion (CSS-grid `0fr->1fr`, `aria-expanded`/`aria-controls`/`role=region`/`aria-labelledby`, chevron rotate). CTA = glass conversion panel with corner-tick framing + 3-step onboarding panel.
- Scoped vanilla JS (inline `<script>` at body end): mobile menu toggle (aria-expanded + icon swap + auto-close on link click), FAQ accordion single-open. CSS-only: `.card` glass hover (border + soft cyan glow + tonal lift), `.link-underline` grow, `.live-dot`/`.live-dot-red` pulse, animated `.edge`/`.edge-hot` topology flow, `.radar-sweep` rotation, `.spark-path` draw, `.mesh-drift`, `.marquee-track` logos.
- **Accessibility**: skip link, semantic landmarks, single h1, ARIA on accordion (labelledby + controls + region), mobile menu aria-expanded, SVG diagrams `role="img"` + `aria-label`, decorative SVGs `aria-hidden`, cyan `:focus-visible` ring. `prefers-reduced-motion` disables mesh drift/radar sweep/marquee/sparkline/edge flow/transitions.
- QA verified: 0 horizontal overflow at 320/375/390/430/768/1024/1280/1440/1920px (index + preview), 0 console errors, accordion single-open + mobile menu interactions pass (aria-expanded sync verified), strict HTML5 valid (html5lib), metadata.json + snippets-index.json valid JSON.
- Registered in `snippets-index.json` as family `Vesper Attack-Surface Management Platform (Template)` (`category: "Templates"`, `tech: "Tailwind CSS"`, `variantsCount: 1`); also under `technologies[Tailwind CSS].families`. Stats after: totalFamilies=90, totalVariants=735.

### `atlas-analytics` ‚Äî Minimal Editorial Analytics multipage application (Tailwind)
- Fictional brand: **Atlas Analytics** ‚Äî enterprise product-analytics platform (overview / analytics explorer / funnels / cohorts / retention / revenue / reports / dashboards / segments / events / settings). Files: `preview.html` (template showcase shell ‚Äî intro, all-11-pages index, design-system summary, composition feature, final CTA), `metadata.json`, `README.md`, `assets/icons/logo.svg` (hairline **[A] mark** ‚Äî framed square, geometric peak, single blue apex dot), `assets/images/og-image.svg`, `pages/{index,analytics,funnels,cohorts,retention,revenue,reports,dashboards,segments,events,settings}.html` (11 pages).
- **A flagship multipage Tailwind application template**, NOT a single `index.html` pretending to be an app. Each page is independently usable and linked by plain relative `<a href="analytics.html">` links ‚Äî open any `pages/*.html` directly and navigate the whole product with no framework, build step, backend or JS application. Reference implementation for **composition** (how dozens of small pieces combine into a coherent product UI).
- Design system (the pasted DevSnips Vanilla design-tokens philosophy, applied to a Tailwind template): **minimal / light / editorial / data-dense** ‚Äî white canvas `#ffffff`, near-black text `#111111`, soft gray secondary `#6b7280`, 1px hairline borders `#e5e7eb`, small radii (4-6px), restrained shadows (`0 1px 2px rgb(0 0 0 / 0.04)`), ONE controlled blue accent `#2563eb` (interactive emphasis / links / primary actions / chart data only ‚Äî never decoration), semantic status colors (ok `#16a34a` / warn `#d97706` / danger `#dc2626` / info `#0ea5e9`) used only for status. **Deliberately NOT** neon/purple-on-black/glassmorphism/gradient-text/glow/animated backgrounds (the anti-AI-aesthetic). Fonts: Inter (display+body) + JetBrains Mono (metrics/labels).
- Token architecture via CSS custom properties: `:root` defines `--ds-*`-style semantic tokens (primitive → semantic → template → component), `[data-theme="dark"]` swaps them for a calm inverted palette (near-black surfaces, soft white text ‚Äî never neon). Components consume semantic helper classes (`.bg-canvas/.bg-surface/.bg-subtle/.text-primary/.text-secondary/.text-muted/.bd/.bd-subtle/.bd-strong/.accent/.bg-accent/.bg-accent-soft`) so dark mode inverts cleanly without touching markup.
- Shared **Atlas shell** inlined per page (copy-paste, no server includes): sticky topbar (logo + env selector + Cmd+K command palette trigger + no-flash persisted dark-mode toggle + avatar) + fixed 248px left sidebar (`Analyze` / `Build` / `Configure` nav groups, `aria-current="page"` active state) → slide-in drawer on `<lg` (hamburger toggle `aria-expanded`/`aria-controls`, backdrop z-40, sidebar z-50, Esc + backdrop close). Page header (breadcrumb + h1 + date-range/actions). Cmd+K command palette (`role="dialog"`/`aria-modal`, filter, Esc).
- 11 pages with realistic content + hand-built inline SVG charts (no charting lib): Overview (KPI grid w/ sparklines, WAU area chart, top sources, activity feed), Analytics (metric trend w/ compare + range/metric switching, events table, distribution bars), Funnels (4-stage funnel SVG w/ drop-off, step table, compare toggle), Cohorts (triangular weekly retention heatmap, 6-tier opacity scale), Retention (overlapping curves by cohort + N-week table), Revenue (MRR/ARR KPIs, trend area chart, plan-breakdown bars, transactions table), Reports (builder prompt + 6 saved-report cards w/ schedule badges), Dashboards (widget grid + add-widget empty state), Segments (audience table w/ trend sparklines + status), Events (frequency table + naming guidance + copyable sample payload), Settings (tabbed Workspace/General/Billing ‚Äî `role="tablist"`, toggles, members, danger zone).
- Scoped vanilla JS (IIFEs per page): theme toggle (persisted, no-flash pre-paint), sidebar drawer (mobile), command palette (open/filter/Esc/⌘K), settings tabs (`role="tab"`/`aria-selected`/`tabpanel`), segment-button groups, copy-to-clipboard. All `prefers-reduced-motion` safe.
- **Accessibility**: skip link, semantic landmarks, single h1 per page, ARIA on drawer/command-palette/tabs/toggle, visible accent `:focus-visible` rings, `prefers-reduced-motion` disables transitions/animations/drawer.
- QA verified (Playwright + html5lib): strict HTML5 valid (html5lib, 0 errors) on all 12 files; 0 horizontal overflow + 0 console errors at 375/768/1024/1280px on all 12 files; inline JS valid (node --check) on all 12 files; interactions pass (dark-mode toggle + persistence, sidebar drawer open/backdrop-close, command palette open/filter/Esc, settings tab switch); repo `scripts/_qa_template.py` PASS on preview; `scripts/validate.py` PASS.
- Registered in `snippets-index.json` as family `Atlas Analytics Platform` (`category: "Templates"`, `tech: "Tailwind CSS"`, `type: "template"`, `variantsCount: 1`, path `Tailwind/Templates/atlas-analytics/`); variant `files` lists README.md, metadata.json, preview.html + all 11 `pages/*.html` (one level deep via `_gen/rebuild_index.py` `make_variant`). Family-level `description` + `searchTerms` were patched in after rebuild (rebuild_index only carries family-level description/searchTerms from the previous index for new families). Stats after: totalFamilies=79, totalVariants=662, tailwindByType={component:253, section:200, template:10}. (rebuild_index also corrected a pre-existing stale section count 201→200.)

## Vanilla Templates — `Vanilla/Templates/`

The Vanilla template collection follows the shared `Vanilla/Templates/design-tokens.md` spec (primitive → semantic → template → component `--ds-*` + `--template-*` token layers; neutral-first, one controlled accent, hairline borders, restrained shadows). Each template folder is a "leaf" in `validate.py` terms (has `metadata.json`, no child folder with `metadata.json`) and is indexed as a single-variant family.

**CURRENT STATE (2026-08-15 consolidation):** the collection was aggressively pruned to **8 high-quality templates** that match (or were rebuilt to match) the restrained, editorial, product-focused design language. 9 off-brand / low-value folders were deleted (glassmorphism, neon, gradients, purple/violet accents, decorative blobs, cyber aesthetics — all banned in `design-tokens.md` §6): `nft-web3-project`, `portfolio-site`, `startup-template`, `product-launch` (old multi-style), `blog-landing-pages`, `micro-saas-product`, `template-element`, `Landing-Pages`, `Standalone`. Two keepers were renamed and rebuilt: `freelancer-portfolio` → `developer-portfolio`, `ai-tool-launch` → `product-launch`.

**Final lineup (8 templates):** SaaS Dashboard (multipage), Documentation Site (modular), Job Board (modular), Agency (modular, dark-first), Developer Portfolio (modular, rebuilt), Product Launch (modular, rebuilt), Event Conference (modular, rebuilt), HTML5 Boilerplate (single-page starter). Stats after: totalFamilies=78, totalVariants=662 (Vanilla 40 families / 199 variants [297 Components + 8 Templates]). Index matches disk exactly.

**Canonical folder layout (every Vanilla template):** the root holds exactly `preview.html` + `metadata.json` + `README.md` (+ optional `agent_instruction.md` for AI adaptation, + optional `assets/`/`css/`/`js/` for shared resources); all code files live in a `pages/` sub-directory:
```
<Template>/
├── pages/              ← all the code files
│   └── index.html      (single-page starters) | code.html + style.css + script.js (modular) | *.html (multi-page)
├── preview.html        ← self-contained single-file preview (modular templates inline CSS+JS); or thin iframe wrapper (html5-boilerplate); or the gallery shell (SaaS Dashboard)
├── metadata.json
├── README.md
└── agent_instruction.md  (present on 7 templates; absent on html5-boilerplate)
```
- **Modular templates** (Agency, Documentation Site, Job Board, Developer Portfolio, Product Launch, Event Conference): `pages/code.html` (HTML structure, links `style.css` + `script.js`) + `pages/style.css` (the `--ds-*` design system) + `pages/script.js` (interactions); `preview.html` is self-contained (inlines CSS+JS via `Vanilla/Templates/_build_preview.py`). All rebuilt templates are light-default with calm opt-in dark mode (no-flash pre-paint + persisted toggle), hairline 1px borders, small controlled radii, single controlled blue accent, Inter + JetBrains Mono, IntersectionObserver scroll-reveal (reduced-motion safe), skip link + semantic landmarks + single `h1` + ARIA + `:focus-visible` + native controls throughout.
- **Single-page starter** (html5-boilerplate): `pages/index.html` holds a minimal HTML5 skeleton with the core `--ds-*` token foundation, no-flash dark-mode script, and reduced-motion guard; root `preview.html` is a thin full-viewport `<iframe src="pages/index.html">` wrapper.
- **SaaS Dashboard** (multipage): the 31 page files live in `pages/`; `css/`, `js/`, `assets/` stay at root as shared resources referenced by the root `preview.html` gallery shell and the pages (via `../`).
`snippets-index.json` variants list both root files (`README.md`, `metadata.json`, `preview.html`) and the one-level `pages/*` contents (via `_gen/rebuild_index.py` `make_variant`, which recurses into `pages/`).

**`agent_instruction.md`** — present on 7 templates (all except html5-boilerplate). Concise AI-adaptation guide: what the template is, the design-system rules, the file layout, how to adapt (rebrand / swap content / add pages), explicit "do not" guardrails, and the quality bar to re-check. Points back to `design-tokens.md` as the source of truth.

**`Vanilla/Templates/_build_preview.py`** — reusable helper that inlines `pages/style.css` + `pages/script.js` into `pages/code.html` to regenerate a self-contained `preview.html` for any modular Vanilla template. Run from inside the template folder: `python3 ../_build_preview.py .`.

**`scripts/_qa_template.py`** — Playwright QA harness for individual Vanilla templates: checks horizontal overflow at 320/375/768/1024/1280/1920px, console/page errors, and template-specific interactions. Run: `python3 scripts/_qa_template.py Vanilla/Templates/<template>/preview.html`.

### `Documentation Site` — editorial developer documentation (modular files, Pico CSS, light-mode-first)
- A premium, minimal, **light-mode-first** **developer documentation** template. **Split into modular files**: `code.html` (HTML structure, links `style.css` + `script.js`) + `style.css` (the design system) + `script.js` (router + content + interactions) + `preview.html` (self-contained single-file preview that inlines CSS+JS so it opens directly) + `metadata.json` + `README.md` + `assets/{logo,favicon}.svg`. Built on **Pico CSS** (CDN) + the shared `--ds-*` token system. **Blue-600 accent** (`#2563eb`) — never violet/neon per `design-tokens.md`. **Light is the default**; dark mode is opt-in (persisted, no-flash pre-paint script).
- **15 sections** via hash routing (`#/home`, `#/introduction`, `#/getting-started`, `#/installation`, `#/quick-start`, `#/vanilla`, `#/components`, `#/templates`, `#/design-tokens`, `#/guides`, `#/api`, `#/examples`, `#/faq`, `#/changelog`, `#/roadmap`, `#/contributing`). Architecture (in `script.js`): `NAV` config → nested sidebar; `PAGES.<id>` → content-builder functions; small runtime → router + scrollspy + search + code tools + theme + drawer. All content lives in `script.js` (no `pages/`). `preview.html` is generated by inlining `style.css` and `script.js` into the `code.html` structure (`_gen`-style build step is a local helper, not committed).
- Design: neutral-first warm-gray ramp, one blue accent, **hairline 1px borders, restrained shadows, small radii, light code blocks** (readable in light mode — not dark-on-light), Inter + JetBrains Mono type scale. Editorial typography (oversized page titles, lead paragraphs, eyebrow labels), no gradients, no glassmorphism, no marketing hero. `preview.html` is the canonical single preview; the modular files are for customization.
- Desktop layout: sticky header + nested left sidebar (4 groups: Start/Library/Reference/Project) + main content + sticky right "On this page" TOC (scrollspy via `IntersectionObserver`). Nested nav with `aria-current` active state + live filter. Breadcrumbs, last-updated + reading-time + edit-on-GitHub meta. Prev/next pager. `Cmd+K`/`/` search command palette (arrow-key nav, Enter, Esc, grouped results w/ match highlighting). Code blocks w/ lightweight vanilla-JS syntax highlighter (HTML/CSS/JS/JSON/Bash, escape-then-tokenize) + copy button + tabbed code blocks (git/curl/wget, ARIA tablist). Five callout variants (info/note/tip/warning/danger). Responsive tables in `overflow-x:auto`. Status badges. API reference blocks (signature + params + example).
- **Light/dark mode**: no-flash system-preference detection (pre-paint inline script in `<head>`) + persisted header toggle. Calm dark palette (near-black surfaces, soft white text — never black + neon purple).
- **Responsive by composition**: sidebar → off-canvas drawer (Esc + backdrop) below 1024px; TOC → collapsible tappable panel below 1180px; header search hidden below 640px; pager stacks to 1 col; code blocks scroll horizontally without page overflow; tables wrap in `overflow-x:auto`. **Zero horizontal overflow at 320–1920px.**
- **Accessibility**: skip link, semantic landmarks, ARIA on search dialog/tablists/drawer/TOC/breadcrumbs, `:focus-visible` rings, keyboard-operable (Esc closes overlays; Cmd/Ctrl+K + `/` open search; arrows navigate results; Enter activates), `prefers-reduced-motion` guards on all animations.
- **Content**: realistic DevSnips docs referencing real repo paths (`Vanilla/Templates/`, `Vanilla/Templates/SaaS Dashboard/`, `Vanilla/Templates/Documentation Site/`, `Vanilla/Templates/design-tokens.md`), the `metadata.json` schema, the `scripts/validate.py` + `scripts/qa_vanilla.py` workflow, and the contribution process.
- QA verified (Playwright) for **both** `code.html` and `preview.html`: 0 console errors, 0 horizontal overflow at 320/375/768/1024/1280/1920px, strict HTML5 valid (html5lib, 0 errors), JS syntax valid (node --check), `metadata.json` valid. Interactions pass: hash routing + active nav, scrollspy TOC, search open/close + result nav, theme toggle, code copy + tab switching, FAQ `<details>` accordion, prev/next nav, mobile drawer open/close.
- Registered in `snippets-index.json` as family `Documentation Site` (`category: "Templates"`, `tech: "Vanilla HTML/CSS/JS"`, `variantsCount: 1`, path `Vanilla/Templates/Documentation Site/`); variant `files` lists the modular files. Inserted after `SaaS Dashboard` and listed under `technologies[Vanilla HTML/CSS/JS].families`. Stats after: totalFamilies=108, totalVariants=854, totalStyles=2018.

### `Agency` — editorial creative-agency marketing site (modular files, Pico CSS, dark-first)
- A premium, **dark-first** editorial creative-agency marketing template. **Split into modular files**: `code.html` (HTML structure, links `style.css` + `script.js`) + `style.css` (the design system) + `script.js` (mobile nav + scroll reveal + scrollspy) + `preview.html` (self-contained single-file preview that inlines CSS+JS so it opens directly) + `metadata.json` + `README.md`. Built on **Pico CSS** (CDN) + the shared `--ds-*` token system. **No `assets/` directory** — all imagery is Unsplash CDN URLs; fonts + Pico CSS are CDN.
- **Dark-first, fixed** theme (no light-mode toggle): black `#000000` bg, white text, single lime accent `#ccff00` (`--ds-color-accent`), gray-100/600/900 neutrals. Inter (400/600/800/900) from Google Fonts. Geometry: `0px` radius (square), `1px solid #222` hairline borders, no rounded surfaces / no drop shadows (flat editorial grid). Single `0.3s cubic-bezier(0.4,0,0,2,1)` motion token; `prefers-reduced-motion` disables smooth-scroll/reveal/transitions. Pico `--pico-*` vars re-pointed at `--ds-*` tokens.
- **13 sections** (single page): sticky Header (logo + anchor nav + primary CTA + mobile menu toggle `aria-expanded`/`aria-controls`), Hero (asymmetric 2-col text + image grid with `clamp()` headline + meta row), About (heading + paragraph + 2-up stats), Services (editorial 6-item numbered list), Selected Work (alternating image/content project layouts via CSS `order`), Case Study (Challenge/Approach/Solution/Results + 16:9 image), Process (5-step grid: Discover/Define/Design/Build/Refine), Capabilities (4-up), Team (4-up square portraits), Testimonials (3-up blockquote cards), Insights (3-up blog cards with category/date/read-time meta), CTA (centered), Footer (4-col + bottom bar).
- Scoped vanilla JS (single inline `<script>` at body end, also in `script.js`): (1) mobile nav toggle (`aria-expanded` + `☰`/`✕` swap + auto-close on link click), (2) IntersectionObserver scroll-reveal (`.ds-reveal` → `.ds-visible`, threshold 0.15, unobserved after), (3) scrollspy active nav (`section[id]` + `aria-current="location"`, threshold 0.4). The head inline `<script>document.documentElement.classList.add('js')</script>` gates reveal (no-JS falls back to visible). No deps.
- **Responsive**: 3 breakpoints (≤768px nav collapses to dropdown + grids stack; 769–1024px grids reflow to 2–3-up; >1024px full multi-col). `overflow-x:hidden` + `max-width:1400px` containers + fluid `clamp()` → **0 horizontal overflow at 320–1920px**. Reduced-motion safe.
- **Accessibility**: skip link, semantic landmarks (header/nav/main/section/article/footer), single h1, `aria-expanded`+`aria-controls` mobile menu, `aria-current` scrollspy, `:focus-visible` accent ring, descriptive alt text, reduced-motion guard.
- QA verified (Playwright) for **both** `code.html` and `preview.html`: 0 console errors, 0 page errors, 0 horizontal overflow at 320/375/768/1024/1280/1920px, strict HTML5 valid (html5lib, 0 errors), `script.js` valid (node --check), `metadata.json` valid. Interactions pass: scroll-reveal (11 `.ds-reveal` → `.ds-visible`) + mobile nav toggle (`aria-expanded` false→true, `.ds-open` applied). Note: bare `&` in the title + Google-Fonts + Unsplash URLs were escaped to `&amp;` (decodes identically; required for html5lib strict validity) — visual/functional output unchanged.
- Registered in `snippets-index.json` as family `Agency` (`category: "Templates"`, `tech: "Vanilla HTML/CSS/JS"`, `variantsCount: 1`, path `Vanilla/Templates/Agency/`); variant `files` = `[code.html, style.css, script.js, preview.html, README.md, metadata.json]`. Listed under `technologies[Vanilla HTML/CSS/JS].families`. ID `agency-001`, slug `agency`, source `DevSnips`, related `[saas-dashboard, documentation-site, portfolio-site]`. Stats after: totalFamilies=109, totalVariants=855, totalStyles=2019.
- **Gotcha**: `script.js` and the inlined `preview.html` `<script>` block contain example code strings with literal `<script>`/`</script>` tags — the closing `</script>` MUST be escaped as `<\/script>` in the JS source or the HTML parser closes the real script block prematurely (the highlighter renders it as a token, so escaping only matters in `preview.html`). Also: `renderPageClean` must add the `active` class to the page element (`.page.active { display:block }`), else content is `display:none`.

### `Job Board` — developer job-board application (modular files, light-default, data-dense)
- A dense, structured **developer job-board** template. **Split into modular files**: `code.html` (HTML structure, links `style.css` + `script.js`) + `style.css` (the design system) + `script.js` (all views, rendering, filtering, pagination, save/apply, mobile nav + filter drawer) + `preview.html` (self-contained single-file preview that inlines CSS+JS so it opens directly) + `metadata.json` + `README.md`. **No `assets/` directory** — company logos are single-letter text avatars, status/meta indicators are inline SVG + emoji, fonts (Inter + JetBrains Mono) are Google Fonts CDN.
- Built on the shared `--ds-*` token system (per `design-tokens.md`): the `:root` block defines the DevSnips `--ds-*` layer (primitive → semantic → template) and maps the template's original `--color-*` vocabulary onto those tokens so the light-mode visual output is **identical to the original uploaded design** (verified 1:1 by Playwright computed-style comparison). **Light is the default**; an opt-in calm dark mode via `prefers-color-scheme` inverts surfaces/text while preserving the blue accent (`#1d4ed8`) and status colors. `prefers-reduced-motion` disables animations and smooth scroll. A generic `.hidden { display:none }` utility is added (the original used `.hidden` on the saved-jobs empty state but only defined `.modal-overlay.hidden`).
- **Single-page view switching** (no reloads): Jobs (search bar + location/type selects + category chips + result count + paginated list, 8 per page), Job Detail (description/responsibilities/requirements/skills/benefits + company sidebar with Apply/Save + details sidebar), Companies (auto-fill grid), Company Detail (about + info sidebar + open positions), Saved Jobs (with empty state), Candidate (sidebar + Profile / Applications status table / Saved tabs). Scoped apply modal (`role=dialog`/`aria-modal`, Esc + overlay close), toast notifications, mobile nav dropdown (`aria-expanded`), mobile filter drawer (overlay + Esc). All data lives in `script.js` (10 companies, 24 jobs, applications).
- Scoped vanilla JS (IIFE at body end, also in `script.js`): document-level click delegation for view switching / job-card / save / apply / chips / pagination / candidate-nav / drawer-chips, plus search debounce, filter-select change handlers, mobile-nav toggle, filter drawer open/close/apply, modal submit, Escape handler, responsive mobile-filter-button visibility on resize. No deps.
- **Responsive**: 4 breakpoints (≤1024px detail grid collapses + sidebar → 2-col; ≤768px nav collapses to dropdown + candidate sidebar → row + company grid → 1-col + location/type selects hide → Filters button; ≤640px search bar compacts to 1-row; ≤480px page/nav padding reduce + search stacks + drawer full-width). **Zero horizontal overflow at 320–1920px.**
- **Accessibility**: semantic landmarks (nav/container/aside/section), ARIA labels on nav/search/selects/modal/icon buttons, `aria-expanded` on mobile toggle, `role=dialog`+`aria-modal`+`aria-labelledby` on apply modal, `role=list`/`role=listitem` on job listings, `:focus-visible` outlines, all native controls (keyboard-operable), status badges convey state via text labels (not color alone), reduced-motion guard.
- QA verified (Playwright) for **both** `code.html` and `preview.html`: 0 console errors, 0 horizontal overflow at 320/375/768/1024/1280/1920px, strict HTML5 valid (html5lib, 0 errors), `script.js` valid (node --check), `metadata.json` valid. Interactions pass: jobs view 8 cards, job detail navigation, apply modal open/submit/toast, companies (10 cards), saved (3 cards), candidate applications (5 rows) + saved (3 cards), mobile nav `aria-expanded` false→true/open, filter drawer open + 8 chips + Esc-close. Light-mode computed styles match the original uploaded file 1:1 (body bg, text, font, nav, button, card, tag, chip, avatar, border all identical).
- Registered in `snippets-index.json` as family `Job Board` (`category: "Templates"`, `tech: "Vanilla HTML/CSS/JS"`, `variantsCount: 1`, path `Vanilla/Templates/Job Board/`); variant `files` = `[code.html, style.css, script.js, preview.html, README.md, metadata.json]`. Listed under `technologies[Vanilla HTML/CSS/JS].families`. ID `job-board-001`, slug `job-board`, source `DevSnips`, related `[saas-dashboard, documentation-site, agency]`. Stats after: totalFamilies=110, totalVariants=856, totalStyles=2020.
