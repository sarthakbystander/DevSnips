# Radio Styled

Styled radio control.

## Usage
```bash
npx devsnips add Tailwind/Components/Input/radio-styled
```
Copy from `code.html`. Prefer native radio + label.

## File Structure
`code.html` · `preview.html` · `metadata.json` · `README.md`
