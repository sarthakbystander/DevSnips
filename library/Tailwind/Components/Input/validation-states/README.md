# Validation States

Input validation state styles (error, success, etc.).

## Usage
```bash
npx devsnips add Tailwind/Components/Input/validation-states
```
Copy from `code.html`. Pair error styles with text messages; do not rely on color alone.

## File Structure
`code.html` · `preview.html` · `metadata.json` · `README.md`
