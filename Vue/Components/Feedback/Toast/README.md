# Toast

Minimal Vue 3 toast notification with semantic status support, optional dismissal, slots, and dark-mode support.

## Responsive behavior

The toast uses a fluid width with a comfortable mobile layout and a max width for larger screens.

## Usage

```vue
<Toast title="Saved" message="Your changes were saved." type="success" @close="visible = false" />
```
