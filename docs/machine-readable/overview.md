# Machine-readable system — overview

DevSnips treats its machine-readable layer as a first-class product surface, not a byproduct. Everything an agent or integrator needs to query the inventory exists as a structured file, and every structured file is generated from one canonical source.

## The surfaces

| Surface | Format | Generated by | Consumers |
|---|---|---|---|
| `snippets-index.json` (the registry) | JSON | `scripts/tooling/indexing/rebuild_index.py` | CLI resolution, validators, website generator, agents, integrators |
| `agents/resources/indexes/components-index.json` | JSON | `scripts/tooling/indexing/build_resource_indexes.py` | Type-scoped agent queries |
| `agents/resources/indexes/sections-index.json` | JSON | same | same |
| `agents/resources/indexes/templates-index.json` | JSON | same | same |
| `website/search-index.json` | JSON | `scripts/tooling/generators/gen_site.py` | Website search; lightweight integrations |
| `website/llms.txt` / `llms-full.txt` | Markdown text | same | LLM-oriented inventory browsing |
| `website/sitemap.xml` | XML | same | URL enumeration |
| `metadata.json` (per resource) | JSON | Authored by contributors | Index input; direct inspection |
| `devsnips/config.json` (in user projects) | JSON | The CLI | Install-state queries inside a project |

## Principles

1. **One source of truth.** `library/` + per-resource `metadata.json` is canonical. Every other surface is derived. Never hand-edit derived files.
2. **Determinism.** Regenerating over an unchanged repository produces identical output (the registry's `lastUpdated` date is the one intentional exception).
3. **Cross-validation over trust.** The primary validator checks registry↔disk agreement in both directions; the registry generator refuses to write when its own validation finds a mismatch.
4. **Portability.** Registry and index paths are tech-first without the `library/` prefix, computed portably (never absolute machine paths). The on-disk location of any entry is `library/<path>`.
5. **Stable identity.** The registry path is the resource's identity. IDs must remain stable once published.

## Choosing a surface

- **Full inventory query** → registry (`snippets-index.json`).
- **Type-scoped query with install commands** → specialized index (`components|sections|templates-index.json`).
- **Name/description/tag search** → `website/search-index.json` or the registry.
- **Compact LLM-readable listing** → `llms.txt` (names + URLs) or `llms-full.txt` (adds descriptions + tags).
- **"What is installed in this project?"** → `devsnips/config.json`.
- **Deep truth about one resource** → the resource's own `metadata.json` at `library/<path>` (registry copies can lag).

## Pages in this section

| Page | Content |
|---|---|
| [Registry](registry.md) | `snippets-index.json` — full real schema with field semantics. |
| [Metadata](metadata.md) | `metadata.json` — per-technology field reference and rules. |
| [Schemas](schemas.md) | Specialized indexes, website surfaces, project config — derived schemas. |

## For integrators

There is no HTTP API. Consume the JSON files directly (raw GitHub URLs or your own mirror of the repository). The registry URL used by the CLI:

```text
https://raw.githubusercontent.com/sarthakbystander/DevSnips/main/snippets-index.json
```

Pin your integration to a commit rather than `main` if you need reproducibility; `main` is the live truth the CLI itself uses.
