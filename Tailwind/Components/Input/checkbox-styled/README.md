# Checkbox Styled

Styled checkbox control.

## Usage
```bash
npx devsnips add Tailwind/Components/Input/checkbox-styled
```
Copy from `code.html`. Prefer native checkbox + label.

## File Structure
`code.html` · `preview.html` · `metadata.json` · `README.md`
