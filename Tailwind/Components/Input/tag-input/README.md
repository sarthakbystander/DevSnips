# Tag Input

Tag / chip input.

## Usage
```bash
npx devsnips add Tailwind/Components/Input/tag-input
```
Copy from `code.html`.

## File Structure
`code.html` · `preview.html` · `metadata.json` · `README.md`
