# Selection Toolbar

Selection toolbar with tooltip-like presentation.

## Usage
```bash
npx devsnips add Tailwind/Components/Tooltips/selection-toolbar
```
Copy from `code.html`.

## File Structure
`code.html` · `preview.html` · `metadata.json` · `README.md`
