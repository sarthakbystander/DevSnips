# Icon Tooltip

Tooltip anchored to an icon button.

## Usage
```bash
npx devsnips add Tailwind/Components/Tooltips/icon-tooltip
```
Copy from `code.html`. Ensure the trigger has an accessible name.

## File Structure
`code.html` · `preview.html` · `metadata.json` · `README.md`
