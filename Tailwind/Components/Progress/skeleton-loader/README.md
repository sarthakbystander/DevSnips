# Skeleton Loader

Skeleton loading placeholder.

## Usage
```bash
npx devsnips add Tailwind/Components/Progress/skeleton-loader
```
Copy from `code.html`. Prefer `aria-busy` on the loading region.

## File Structure
`code.html` · `preview.html` · `metadata.json` · `README.md`
