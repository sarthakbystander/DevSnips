/* DevSnips React section JavaScript parity entrypoint.
 * TypeScript remains the authored source of truth; this entrypoint re-exports
 * the same public API so JS consumers can use the section without duplicating
 * implementation logic.
 */
export * from "./code.tsx";
